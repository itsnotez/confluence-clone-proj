---
phase: 01-core
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/company/wiki/space/entity/Space.java
  - backend/src/main/java/com/company/wiki/space/entity/SpaceFavorite.java
  - backend/src/main/java/com/company/wiki/space/repository/SpaceRepository.java
  - backend/src/main/java/com/company/wiki/space/repository/SpaceFavoriteRepository.java
  - backend/src/main/java/com/company/wiki/space/dto/SpaceDto.java
  - backend/src/main/java/com/company/wiki/space/service/SpaceService.java
  - backend/src/main/java/com/company/wiki/space/controller/SpaceController.java
  - backend/src/main/java/com/company/wiki/common/exception/ErrorCode.java
  - backend/src/test/java/com/company/wiki/space/controller/SpaceControllerTest.java
autonomous: true
requirements: [REQ-SPACE]
must_haves:
  truths:
    - "POST /spaces 로 새 Space를 생성할 수 있다"
    - "GET /spaces 로 활성 Space 목록을 조회할 수 있다"
    - "GET /spaces/{key} 로 특정 Space를 조회할 수 있다"
    - "PUT /spaces/{key} 로 Space를 수정할 수 있다"
    - "DELETE /spaces/{key} 로 Space를 soft delete할 수 있다"
    - "POST /spaces/{key}/favorite 로 즐겨찾기를 추가/제거할 수 있다"
  artifacts:
    - path: backend/src/main/java/com/company/wiki/space/entity/Space.java
      provides: Space 엔티티 (DB 매핑)
      min_lines: 50
    - path: backend/src/main/java/com/company/wiki/space/service/SpaceService.java
      provides: Space CRUD 비즈니스 로직
      min_lines: 80
    - path: backend/src/main/java/com/company/wiki/space/controller/SpaceController.java
      provides: Space REST 엔드포인트
      min_lines: 60
  key_links:
    - from: backend/src/main/java/com/company/wiki/space/controller/SpaceController.java
      to: backend/src/main/java/com/company/wiki/space/service/SpaceService.java
      via: 의존성 주입
      pattern: "SpaceService"
---

<objective>
Space CRUD API와 즐겨찾기 기능을 TDD로 구현한다.

Purpose: Space는 콘텐츠 컨테이너이자 권한 단위 — Phase 1의 핵심 도메인.
Output: Space CRUD + 즐겨찾기 API + 통합 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/src/main/java/com/company/wiki/user/entity/User.java
@backend/src/main/java/com/company/wiki/user/repository/UserRepository.java
@backend/src/main/java/com/company/wiki/common/exception/ErrorCode.java
@backend/src/main/java/com/company/wiki/common/response/ApiResponse.java
@backend/src/main/resources/db/migration/V2__spaces.sql
@backend/src/main/java/com/company/wiki/auth/security/UserDetailsServiceImpl.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Space/SpaceFavorite 엔티티 및 레포지토리</name>
  <files>
    backend/src/main/java/com/company/wiki/space/entity/Space.java,
    backend/src/main/java/com/company/wiki/space/entity/SpaceFavorite.java,
    backend/src/main/java/com/company/wiki/space/repository/SpaceRepository.java,
    backend/src/main/java/com/company/wiki/space/repository/SpaceFavoriteRepository.java
  </files>
  <read_first>
    backend/src/main/resources/db/migration/V2__spaces.sql,
    backend/src/main/java/com/company/wiki/user/entity/User.java
  </read_first>
  <action>
Space.java (@Entity @Table(name="spaces")):
- id(Long IDENTITY), spaceKey(VARCHAR 50, UNIQUE, NOT NULL), name(VARCHAR 300, NOT NULL)
- description(TEXT), type(VARCHAR 20 "PUBLIC"|"PRIVATE", 기본 "PRIVATE"), status(VARCHAR 20 "ACTIVE"|"ARCHIVED"|"DELETED", 기본 "ACTIVE")
- iconEmoji(VARCHAR 10)
- @ManyToOne(fetch=LAZY) @JoinColumn(name="created_by") User createdBy
- createdAt, updatedAt(LocalDateTime), deletedAt(LocalDateTime nullable)
- @PrePersist: createdAt = updatedAt = now(), status/type 기본값 설정
- @PreUpdate: updatedAt = now()

SpaceFavorite.java (@Entity @Table(name="space_favorites")):
- @EmbeddedId SpaceFavoriteId id (spaceId Long, userId Long) — @Embeddable record
- @ManyToOne(fetch=LAZY) @MapsId("spaceId") Space space
- @ManyToOne(fetch=LAZY) @MapsId("userId") User user
- createdAt(LocalDateTime)

SpaceRepository: JpaRepository<Space, Long>
- Optional<Space> findBySpaceKeyAndDeletedAtIsNull(String spaceKey)
- List<Space> findByStatusAndDeletedAtIsNull(String status)
- boolean existsBySpaceKey(String spaceKey)

SpaceFavoriteRepository: JpaRepository<SpaceFavorite, SpaceFavoriteId>
- boolean existsBySpaceIdAndUserId(Long spaceId, Long userId)
- void deleteBySpaceIdAndUserId(Long spaceId, Long userId)
- List<SpaceFavorite> findByUserId(Long userId)
  </action>
  <verify>cd backend && export PATH="/opt/homebrew/opt/openjdk@21/bin:$PATH" && ./mvnw compile -q 2>&1 | tail -5</verify>
  <acceptance_criteria>
    - Space.java에 @Entity, spaceKey 필드 존재
    - SpaceFavorite에 @EmbeddedId 존재
    - 컴파일 성공
  </acceptance_criteria>
  <done>엔티티 컴파일 성공</done>
</task>

<task type="auto">
  <name>Task 2: SpaceDto + ErrorCode 보완</name>
  <files>
    backend/src/main/java/com/company/wiki/space/dto/SpaceDto.java,
    backend/src/main/java/com/company/wiki/common/exception/ErrorCode.java
  </files>
  <read_first>
    backend/src/main/java/com/company/wiki/common/exception/ErrorCode.java,
    backend/src/main/java/com/company/wiki/space/entity/Space.java
  </read_first>
  <action>
SpaceDto.java:
- CreateRequest: spaceKey(NotBlank, Pattern("[A-Z0-9_]{2,50}")), name(NotBlank), description, type("PUBLIC"|"PRIVATE", 기본 "PRIVATE"), iconEmoji
- UpdateRequest: name, description, type, iconEmoji (모두 Optional)
- Response: id, spaceKey, name, description, type, status, iconEmoji, createdBy(UserSummary), createdAt, favorited(boolean, 현재 사용자의 즐겨찾기 여부)
  - UserSummary: id, name
- ArchiveRequest: 없음 (별도 endpoint)
- static Response from(Space space, boolean favorited)

ErrorCode에 추가 (이미 있으면 skip):
- SPACE_NOT_FOUND 이미 있음 — 확인만
- ARCHIVED_SPACE 이미 있음 — 확인만
  </action>
  <verify>cd backend && export PATH="/opt/homebrew/opt/openjdk@21/bin:$PATH" && ./mvnw compile -q 2>&1 | tail -5</verify>
  <acceptance_criteria>
    - SpaceDto.CreateRequest에 spaceKey 필드 존재
    - SpaceDto.Response.from(Space, boolean) 존재
    - 컴파일 성공
  </acceptance_criteria>
  <done>SpaceDto 완성, 컴파일 성공</done>
</task>

<task type="auto">
  <name>Task 3: SpaceService 구현</name>
  <files>
    backend/src/main/java/com/company/wiki/space/service/SpaceService.java
  </files>
  <read_first>
    backend/src/main/java/com/company/wiki/space/repository/SpaceRepository.java,
    backend/src/main/java/com/company/wiki/space/dto/SpaceDto.java,
    backend/src/main/java/com/company/wiki/user/repository/UserRepository.java
  </read_first>
  <action>
SpaceService.java (@Service @Transactional):
- List<SpaceDto.Response> findAll(Long currentUserId)
  → findByStatusAndDeletedAtIsNull("ACTIVE"), 각각 favorited = SpaceFavoriteRepository.existsBySpaceIdAndUserId
- SpaceDto.Response findByKey(String spaceKey, Long currentUserId) [SPACE_NOT_FOUND]
- SpaceDto.Response create(SpaceDto.CreateRequest req, Long createdById)
  → existsBySpaceKey → DUPLICATE_SPACE_KEY
  → User 조회(createdBy), Space 저장
- SpaceDto.Response update(String spaceKey, SpaceDto.UpdateRequest req, Long currentUserId)
  → status=="ARCHIVED" → ARCHIVED_SPACE
  → null이 아닌 필드만 업데이트
- void delete(String spaceKey)
  → space.setDeletedAt(LocalDateTime.now()), status="DELETED"
- void toggleFavorite(String spaceKey, Long userId)
  → 이미 즐겨찾기 → 삭제, 없으면 → 추가
  </action>
  <verify>cd backend && export PATH="/opt/homebrew/opt/openjdk@21/bin:$PATH" && ./mvnw compile -q 2>&1 | tail -5</verify>
  <acceptance_criteria>
    - SpaceService에 findAll, findByKey, create, update, delete, toggleFavorite 메서드 존재
    - 컴파일 성공
  </acceptance_criteria>
  <done>SpaceService 완성, 컴파일 성공</done>
</task>

<task type="auto">
  <name>Task 4: SpaceController REST 엔드포인트</name>
  <files>
    backend/src/main/java/com/company/wiki/space/controller/SpaceController.java
  </files>
  <read_first>
    backend/src/main/java/com/company/wiki/space/service/SpaceService.java,
    backend/src/main/java/com/company/wiki/user/controller/UserController.java
  </read_first>
  <action>
SpaceController.java (@RestController @RequestMapping("/spaces") @RequiredArgsConstructor):
- getCurrentUserId(UserDetails principal): Long.parseLong(principal.getUsername())
- GET /spaces → findAll(currentUserId)
- GET /spaces/{spaceKey} → findByKey(spaceKey, currentUserId)
- POST /spaces → create(@Valid req, currentUserId)
- PUT /spaces/{spaceKey} → update(spaceKey, @Valid req, currentUserId)
- DELETE /spaces/{spaceKey} → delete(spaceKey), 204 No Content
- POST /spaces/{spaceKey}/favorite → toggleFavorite(spaceKey, currentUserId), 200 OK
- DELETE /spaces/{spaceKey}/favorite → toggleFavorite(spaceKey, currentUserId), 200 OK

모든 엔드포인트: @AuthenticationPrincipal UserDetails principal 주입
반환타입: ResponseEntity<ApiResponse<T>> 또는 ApiResponse<T>
  </action>
  <verify>cd backend && export PATH="/opt/homebrew/opt/openjdk@21/bin:$PATH" && ./mvnw compile -q 2>&1 | tail -5</verify>
  <acceptance_criteria>
    - SpaceController에 GET /spaces, POST /spaces, DELETE /spaces/{spaceKey}/favorite 존재
    - 컴파일 성공
  </acceptance_criteria>
  <done>SpaceController 완성, 컴파일 성공</done>
</task>

<task type="auto">
  <name>Task 5: TDD — Space API 통합 테스트</name>
  <files>
    backend/src/test/java/com/company/wiki/space/controller/SpaceControllerTest.java
  </files>
  <read_first>
    backend/src/test/java/com/company/wiki/auth/controller/AuthControllerTest.java,
    backend/src/main/java/com/company/wiki/space/dto/SpaceDto.java
  </read_first>
  <action>
SpaceControllerTest.java (@SpringBootTest @AutoConfigureMockMvc @ActiveProfiles("local") @Transactional):

헬퍼: String adminToken() — POST /auth/login { admin/Admin1234! }

테스트:
1. createSpace_success(): POST /spaces { spaceKey:"TEST", name:"테스트 Space", type:"PRIVATE" }
   → 200, data.spaceKey == "TEST"
2. createSpace_duplicateKey_returns409(): 같은 spaceKey로 두 번 → 409 Conflict
3. getSpaces_returnsActivelist(): 스페이스 생성 후 GET /spaces → 200, data 배열에 해당 space 포함
4. deleteSpace_softDelete(): DELETE /spaces/TEST → 204, 이후 GET /spaces/TEST → 404
5. toggleFavorite_addsAndRemoves(): POST /spaces/TEST/favorite → 200, 다시 POST → 즐겨찾기 제거

실행: cd backend && export PATH="/opt/homebrew/opt/openjdk@21/bin:$PATH" && ./mvnw test -Dtest=SpaceControllerTest -Dspring.profiles.active=local 2>&1 | tail -20
  </action>
  <verify>cd backend && export PATH="/opt/homebrew/opt/openjdk@21/bin:$PATH" && ./mvnw test -Dtest=SpaceControllerTest -Dspring.profiles.active=local 2>&1 | tail -10</verify>
  <acceptance_criteria>
    - Tests run: 5 이상
    - BUILD SUCCESS
    - Failures: 0
  </acceptance_criteria>
  <done>모든 테스트 통과, BUILD SUCCESS</done>
</task>

</tasks>

<verification>
- [ ] cd backend && export PATH="/opt/homebrew/opt/openjdk@21/bin:$PATH" && ./mvnw test -Dtest=SpaceControllerTest -Dspring.profiles.active=local 2>&1 | grep -E "Tests run|BUILD"
- [ ] Space CRUD 동작 확인 (create → get → delete)
- [ ] 즐겨찾기 토글 동작 확인
</verification>

<success_criteria>
- 모든 테스트 통과 (BUILD SUCCESS)
- Space CRUD API 동작
- 즐겨찾기 추가/제거 동작
- 미인증 요청 401 반환
</success_criteria>

<output>
After completion, create `.planning/phases/01-core/01-02-SUMMARY.md`
</output>
