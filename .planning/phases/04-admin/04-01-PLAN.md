---
phase: 04-admin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/company/wiki/auditlog/entity/AuditLog.java
  - backend/src/main/java/com/company/wiki/auditlog/repository/AuditLogRepository.java
  - backend/src/main/java/com/company/wiki/auditlog/dto/AuditLogDto.java
  - backend/src/main/java/com/company/wiki/auditlog/service/AuditLogService.java
  - backend/src/main/java/com/company/wiki/common/exception/ErrorCode.java
  - backend/src/main/java/com/company/wiki/permission/service/PermissionService.java
  - backend/src/main/java/com/company/wiki/space/service/SpaceService.java
  - backend/src/main/java/com/company/wiki/mail/service/MailAccountService.java
  - backend/src/test/java/com/company/wiki/auditlog/service/AuditLogServiceTest.java
autonomous: true
requirements:
  - AUDIT-01

must_haves:
  truths:
    - "Space 삭제 이후 audit_logs 테이블에 SPACE_DELETE 레코드가 존재한다"
    - "권한 변경(grantSpacePermission) 이후 audit_logs에 PERMISSION_CHANGE 레코드가 존재한다"
    - "메일계정 생성/삭제 이후 audit_logs에 MAIL_ACCOUNT_CREATE/DELETE 레코드가 존재한다"
    - "AuditLogService.record() 내부 예외가 호출 서비스의 트랜잭션을 롤백시키지 않는다"
  artifacts:
    - path: "backend/src/main/java/com/company/wiki/auditlog/entity/AuditLog.java"
      provides: "audit_logs 테이블 JPA 엔티티"
      contains: "class AuditLog"
    - path: "backend/src/main/java/com/company/wiki/auditlog/service/AuditLogService.java"
      provides: "감사로그 기록 단일 진입점"
      exports: ["record"]
    - path: "backend/src/test/java/com/company/wiki/auditlog/service/AuditLogServiceTest.java"
      provides: "AUDIT-01 통합 테스트"
      contains: "spaceDelete_createsAuditLog"
  key_links:
    - from: "PermissionService.grantSpacePermission()"
      to: "AuditLogService.record(PERMISSION_CHANGE)"
      via: "직접 호출 (try-catch 래핑)"
    - from: "SpaceService.delete()"
      to: "AuditLogService.record(SPACE_DELETE)"
      via: "직접 호출 (try-catch 래핑)"
    - from: "MailAccountService.create/delete"
      to: "AuditLogService.record(MAIL_ACCOUNT_CREATE/DELETE)"
      via: "직접 호출 (try-catch 래핑)"
---

<objective>
감사로그 도메인의 전체 백엔드를 구현한다: AuditLog 엔티티/리포지토리/서비스를 신규 생성하고, 기록 대상인 PermissionService·SpaceService·MailAccountService에 AuditLogService.record() 직접 호출을 통합한다.

Purpose: 권한 변경·Space 삭제·메일계정 등록/삭제 등 주요 행위의 감사 추적을 가능하게 한다. Wave 2의 AdminController가 AuditLogService.findByFilter()를 호출하므로 이 플랜이 먼저 완료되어야 한다.

Output: AuditLog 엔티티, AuditLogRepository, AuditLogDto, AuditLogService (record + findByFilter), 기존 서비스 3개 통합, 통합 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-admin/04-CONTEXT.md
@.planning/phases/04-admin/04-RESEARCH.md

<interfaces>
<!-- V7 audit_logs 스키마 (이미 마이그레이션 완료 — Flyway 스크립트 추가 불필요) -->
<!--
  audit_logs(
    id BIGSERIAL PK,
    actor_id BIGINT NOT NULL REFERENCES users(id),
    action_type VARCHAR(100) NOT NULL,
    target_type VARCHAR(50) NOT NULL,
    target_id BIGINT,
    detail JSONB,
    is_admin_access BOOLEAN NOT NULL DEFAULT FALSE,
    created_at TIMESTAMP NOT NULL DEFAULT NOW()
  )
-->

<!-- ErrorCode 패턴 (ErrorCode.java) -->
<!--
  enum ErrorCode { ..., NOTIFICATION_NOT_FOUND(HttpStatus.NOT_FOUND, "알림을 찾을 수 없습니다."), ... }
  이 플랜에서 NOTIFICATION_NOT_FOUND 추가 필요
-->

<!-- CommentService 서비스 패턴 (검증됨) -->
<!--
  @Service @Transactional @RequiredArgsConstructor
  BusinessException(ErrorCode.XXX) 패턴
  private Long getCurrentUserId(UserDetails principal) { return Long.parseLong(principal.getUsername()); }
-->

<!-- SpaceController ApiResponse 패턴 (검증됨) -->
<!--
  return ApiResponse.ok(service.method());
  @AuthenticationPrincipal UserDetails principal
-->

<!-- PermissionService.grantSpacePermission() 시그니처 -->
<!--
  @Transactional
  public SpacePermission grantSpacePermission(Long spaceId, String subjectType, Long subjectId, String permissionLevel)
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AuditLog 엔티티·리포지토리·DTO·서비스 생성</name>
  <files>
    backend/src/main/java/com/company/wiki/auditlog/entity/AuditLog.java,
    backend/src/main/java/com/company/wiki/auditlog/repository/AuditLogRepository.java,
    backend/src/main/java/com/company/wiki/auditlog/dto/AuditLogDto.java,
    backend/src/main/java/com/company/wiki/auditlog/service/AuditLogService.java,
    backend/src/main/java/com/company/wiki/common/exception/ErrorCode.java,
    backend/src/test/java/com/company/wiki/auditlog/service/AuditLogServiceTest.java
  </files>
  <read_first>
    - backend/src/main/resources/db/migration/V7__audit_notifications.sql (스키마 확인 — 컬럼명/타입 정확히 매핑)
    - backend/src/main/java/com/company/wiki/common/exception/ErrorCode.java (에러코드 추가 위치)
    - backend/src/main/java/com/company/wiki/comment/service/CommentService.java (@Service 패턴 참조)
    - backend/src/main/java/com/company/wiki/common/response/ApiResponse.java (응답 래퍼 확인)
  </read_first>
  <behavior>
    - Test 1: AuditLogService.record()를 호출하면 audit_logs 테이블에 레코드가 저장된다
    - Test 2: AuditLogService.record() 내부에서 예외가 발생해도 호출자의 트랜잭션이 롤백되지 않는다 (try-catch 내부 예외 처리)
    - Test 3: AuditLogService.findByFilter(actorId=null, actionType="SPACE_DELETE", from=null, to=null, pageable)는 SPACE_DELETE 레코드만 반환한다
    - Test 4: AuditLogService.findByFilter()는 기간 필터(from/to)를 적용하여 범위 밖 레코드를 제외한다
  </behavior>
  <action>
    1. AuditLog 엔티티를 com.company.wiki.auditlog.entity 패키지에 생성한다. 필드: id (BIGSERIAL), actorId (actor_id), actionType (action_type, VARCHAR 100), targetType (target_type, VARCHAR 50), targetId (target_id nullable), detail (JSONB — @Column(columnDefinition="jsonb")), isAdminAccess (is_admin_access), createdAt (created_at). @Entity, @Table(name="audit_logs"), @Getter, @Builder, @NoArgsConstructor, @AllArgsConstructor 적용. @PrePersist로 createdAt = LocalDateTime.now() 설정.

    2. AuditLogRepository를 com.company.wiki.auditlog.repository 패키지에 생성한다. JpaRepository&lt;AuditLog, Long&gt; 확장. findByFilter JPQL @Query 추가:
       SELECT a FROM AuditLog a WHERE (:actorId IS NULL OR a.actorId = :actorId) AND (:actionType IS NULL OR a.actionType = :actionType) AND (:from IS NULL OR a.createdAt >= :from) AND (:to IS NULL OR a.createdAt <= :to) ORDER BY a.createdAt DESC
       파라미터: actorId, actionType, from, to (모두 nullable), Pageable pageable.

    3. AuditLogDto를 com.company.wiki.auditlog.dto 패키지에 생성한다. 내부 record Response(Long id, Long actorId, String actionType, String targetType, Long targetId, String detail, boolean isAdminAccess, LocalDateTime createdAt). AuditLog 엔티티로부터 Response를 생성하는 정적 팩터리 메서드 from(AuditLog) 포함.

    4. AuditLogService를 com.company.wiki.auditlog.service 패키지에 생성한다.
       - record(Long actorId, String actionType, String targetType, Long targetId, Object detail, boolean isAdminAccess): try { ObjectMapper.writeValueAsString(detail)로 JSON 직렬화 후 AuditLog.builder()...build() 저장 } catch (Exception e) { log.error("AuditLog record failed: {}", e.getMessage()); } — 예외를 절대 전파하지 않는다.
       - findByFilter(Long actorId, String actionType, LocalDateTime from, LocalDateTime to, Pageable pageable): PagedResponse&lt;AuditLogDto.Response&gt; 반환 (Page를 직접 반환하지 않음). PagedResponse record를 동일 파일에 정의: record PagedResponse&lt;T&gt;(List&lt;T&gt; content, int page, int size, long totalElements, int totalPages).
       - @Service, @RequiredArgsConstructor 적용. ObjectMapper는 생성자 주입.

    5. ErrorCode에 NOTIFICATION_NOT_FOUND(HttpStatus.NOT_FOUND, "알림을 찾을 수 없습니다.") 추가 (Wave 1 알림 플랜(04-02)에서 사용).

    6. AuditLogServiceTest를 @SpringBootTest + @AutoConfigureMockMvc + @ActiveProfiles("local") + @Transactional 로 작성. 테스트 시나리오: (a) record() 호출 후 auditLogRepository.findAll()에 레코드 존재 확인 (b) findByFilter(actionType="SPACE_DELETE") 필터 결과 확인.
  </action>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@21 ./mvnw test -pl backend -Dtest=AuditLogServiceTest -f /Users/shinwon/confluence-clone-proj/pom.xml</automated>
  </verify>
  <acceptance_criteria>
    - AuditLog.java 파일이 존재하고 @Entity @Table(name="audit_logs") 어노테이션을 포함한다
    - AuditLogService.java에 void record( 시그니처가 존재한다
    - AuditLogService.java에 try { 블록이 존재하여 예외를 삼킨다 (catch 블록에서 log.error만 수행)
    - AuditLogRepository.java에 @Query와 findByFilter 메서드가 존재한다
    - ErrorCode.java에 NOTIFICATION_NOT_FOUND 항목이 존재한다
    - AuditLogServiceTest 테스트가 BUILD SUCCESS로 완료된다
  </acceptance_criteria>
  <done>AuditLog 도메인 전체(엔티티/리포지토리/DTO/서비스) 생성 완료, 통합 테스트 GREEN</done>
</task>

<task type="auto">
  <name>Task 2: 기존 서비스에 AuditLogService 통합 (PermissionService, SpaceService, MailAccountService)</name>
  <files>
    backend/src/main/java/com/company/wiki/permission/service/PermissionService.java,
    backend/src/main/java/com/company/wiki/space/service/SpaceService.java,
    backend/src/main/java/com/company/wiki/mail/service/MailAccountService.java
  </files>
  <read_first>
    - backend/src/main/java/com/company/wiki/permission/service/PermissionService.java (grantSpacePermission, revokeSpacePermission 시그니처)
    - backend/src/main/java/com/company/wiki/space/service/SpaceService.java (delete 메서드 시그니처)
    - backend/src/main/java/com/company/wiki/mail/service/MailAccountService.java (create, delete 메서드 시그니처)
    - backend/src/main/java/com/company/wiki/auditlog/service/AuditLogService.java (Task 1에서 생성한 record() 시그니처 확인)
  </read_first>
  <action>
    1. PermissionService에 AuditLogService를 @RequiredArgsConstructor 주입한다. grantSpacePermission() 메서드 내에서 save 직후 다음을 추가한다:
       try { auditLogService.record(actorId, "PERMISSION_CHANGE", "PERMISSION", spaceId, Map.of("subjectType", subjectType, "subjectId", subjectId, "permissionLevel", permissionLevel), false); } catch (Exception e) { log.warn("audit failed: {}", e.getMessage()); }
       단, grantSpacePermission 시그니처에 actorId가 없으면 HttpServletRequest를 주입받거나, SecurityContextHolder.getContext().getAuthentication()에서 userId를 추출한다. 기존 시그니처를 변경하는 대신 SecurityContextHolder에서 추출하는 방식을 사용한다: String name = SecurityContextHolder.getContext().getAuthentication().getName(); Long actorId = Long.parseLong(name);

    2. SpaceService.delete() 메서드에서 실제 삭제(soft delete 포함) 직후 다음을 추가한다:
       try { auditLogService.record(actorId, "SPACE_DELETE", "SPACE", space.getId(), Map.of("spaceKey", space.getKey(), "name", space.getName()), false); } catch (Exception e) { log.warn("audit failed: {}", e.getMessage()); }
       actorId 추출은 SecurityContextHolder.getContext().getAuthentication().getName()에서 Long.parseLong으로 변환한다. delete() 메서드 시그니처가 actorId를 이미 받는 경우 그것을 사용한다.

    3. MailAccountService의 create() 메서드 완료 후:
       try { auditLogService.record(actorId, "MAIL_ACCOUNT_CREATE", "MAIL_ACCOUNT", savedAccount.getId(), Map.of("emailAddress", savedAccount.getEmailAddress()), true); } catch (Exception e) { log.warn("audit failed: {}", e.getMessage()); }
       delete() 메서드 완료 후:
       try { auditLogService.record(actorId, "MAIL_ACCOUNT_DELETE", "MAIL_ACCOUNT", accountId, Map.of("emailAddress", account.getEmailAddress()), true); } catch (Exception e) { log.warn("audit failed: {}", e.getMessage()); }
       actorId는 SecurityContextHolder에서 추출한다.

    4. 세 서비스 모두 AuditLogService와 ObjectMapper를 import하고, try-catch로 감싸서 AuditLogService 호출 실패가 본 트랜잭션을 롤백시키지 않도록 한다.
  </action>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@21 ./mvnw compile -pl backend -f /Users/shinwon/confluence-clone-proj/pom.xml</automated>
  </verify>
  <acceptance_criteria>
    - PermissionService.java에 auditLogService.record( 호출이 존재하고 try-catch 블록으로 감싸져 있다
    - SpaceService.java에 auditLogService.record( 호출이 존재하고 try-catch 블록으로 감싸져 있다
    - MailAccountService.java에 auditLogService.record( 호출이 두 곳(create, delete) 이상 존재한다
    - mvnw compile이 BUILD SUCCESS로 완료된다 (컴파일 에러 없음)
  </acceptance_criteria>
  <done>기존 서비스 3곳에 AuditLogService 통합 완료, 컴파일 성공</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 기존 서비스 → AuditLogService | 내부 서버 호출 — 신뢰할 수 있으나 예외 전파 방지 필수 |
| AuditLogService → PostgreSQL | JPA를 통한 DB 접근 — SQL 인젝션 없음 (파라미터 바인딩) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | AuditLogRepository | mitigate | delete 메서드 노출 금지 — AuditLogRepository에 deleteBy* 메서드를 정의하지 않는다 |
| T-04-01-02 | Tampering | AuditLog.detail JSONB | mitigate | ObjectMapper.writeValueAsString()으로 직렬화 — 직접 SQL 문자열 조합 금지 |
| T-04-01-03 | Denial of Service | AuditLogService.record() 예외 | mitigate | try-catch로 예외 삼킴 — 감사로그 실패가 본 트랜잭션을 롤백시키지 않음 |
| T-04-01-SC | Tampering | npm/pip/cargo installs | accept | 이 플랜은 신규 패키지 설치 없음 |
</threat_model>

<verification>
1. AuditLogServiceTest 실행 — BUILD SUCCESS, 테스트 모두 GREEN
2. 기존 서비스 수정 후 컴파일 성공 확인: `JAVA_HOME=/opt/homebrew/opt/openjdk@21 ./mvnw compile -pl backend`
3. audit_logs 테이블에 레코드가 쌓이는지 수동 확인 (선택): Space 삭제 후 `SELECT * FROM audit_logs WHERE action_type = 'SPACE_DELETE'`
</verification>

<success_criteria>
- AuditLog 도메인(entity/repository/dto/service) 신규 생성 완료
- AuditLogService.record()가 try-catch로 예외를 삼키고 본 트랜잭션에 영향을 주지 않음
- PermissionService, SpaceService, MailAccountService에서 AuditLogService 직접 호출 통합
- AuditLogServiceTest 통합 테스트 GREEN
</success_criteria>

<output>
Create `.planning/phases/04-admin/04-01-SUMMARY.md` when done
</output>
