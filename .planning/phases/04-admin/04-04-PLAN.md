---
phase: 04-admin
plan: 04
type: execute
wave: 2
depends_on:
  - 04-02
files_modified:
  - backend/src/main/java/com/company/wiki/notification/controller/NotificationController.java
  - backend/src/test/java/com/company/wiki/notification/controller/NotificationControllerTest.java
autonomous: true
requirements:
  - NOTIF-02

must_haves:
  truths:
    - "인증된 사용자가 GET /api/notifications 호출 시 200과 본인 알림 목록을 페이징으로 반환한다"
    - "GET /api/notifications/unread-count는 미읽음 알림 수를 숫자로 반환한다"
    - "PATCH /api/notifications/{id}/read는 본인 알림만 읽음 처리하고 200을 반환한다"
    - "PATCH /api/notifications/read-all은 본인의 모든 알림을 읽음 처리하고 200을 반환한다"
    - "다른 사용자의 알림 ID로 PATCH /api/notifications/{id}/read 시 404를 반환한다"
  artifacts:
    - path: "backend/src/main/java/com/company/wiki/notification/controller/NotificationController.java"
      provides: "알림 조회/읽음처리 REST API"
      contains: "class NotificationController"
    - path: "backend/src/test/java/com/company/wiki/notification/controller/NotificationControllerTest.java"
      provides: "NOTIF-02 통합 테스트"
      contains: "getNotifications_returns200"
  key_links:
    - from: "GET /api/notifications"
      to: "NotificationService.getNotifications(userId, isRead, pageable)"
      via: "JWT @AuthenticationPrincipal 추출 userId"
    - from: "PATCH /api/notifications/{id}/read"
      to: "NotificationService.markAsRead(id, userId)"
      via: "userId 소유자 검증 — 04-02 NotificationService IDOR 방지 로직"
---

<objective>
알림 조회/읽음처리 REST API 컨트롤러를 구현하고 통합 테스트를 완성한다. Wave 1(04-02)에서 생성된 NotificationService를 HTTP 엔드포인트로 노출한다.

Purpose: 프론트엔드 알림 벨 아이콘(Wave 3, 04-06)이 소비하는 4개 엔드포인트를 제공한다.

Output: NotificationController, NotificationControllerTest
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-admin/04-CONTEXT.md
@.planning/phases/04-admin/04-RESEARCH.md
@.planning/phases/04-admin/04-02-SUMMARY.md

<interfaces>
<!-- NotificationService (04-02에서 생성) 메서드 시그니처 -->
<!--
  getNotifications(Long userId, Boolean isRead, Pageable pageable): PagedResponse<NotificationDto.Response>
  getUnreadCount(Long userId): long
  markAsRead(Long notificationId, Long userId): void (없으면 BusinessException NOTIFICATION_NOT_FOUND)
  markAllAsRead(Long userId): void
-->

<!-- NotificationDto.Response 필드 -->
<!--
  record Response(Long id, Long userId, String type, String title, String message,
                  boolean isRead, String linkUrl, LocalDateTime createdAt)
-->

<!-- SpaceController 컨트롤러 패턴 (검증됨) -->
<!--
  @RestController @RequestMapping("/notifications") @RequiredArgsConstructor
  return ApiResponse.ok(service.method());
  @AuthenticationPrincipal UserDetails principal
  private Long getCurrentUserId(UserDetails p) { return Long.parseLong(p.getUsername()); }
  private String getCurrentUserRole(UserDetails p) { return p.getAuthorities()...getAuthority(); }
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NotificationController 생성 + 통합 테스트</name>
  <files>
    backend/src/main/java/com/company/wiki/notification/controller/NotificationController.java,
    backend/src/test/java/com/company/wiki/notification/controller/NotificationControllerTest.java
  </files>
  <read_first>
    - backend/src/main/java/com/company/wiki/notification/service/NotificationService.java (04-02에서 생성한 메서드 시그니처 확인)
    - backend/src/main/java/com/company/wiki/notification/dto/NotificationDto.java (Response 필드 확인)
    - backend/src/main/java/com/company/wiki/space/controller/SpaceController.java (컨트롤러 패턴 — @AuthenticationPrincipal, getCurrentUserId 패턴)
    - backend/src/test/java/com/company/wiki/search/SearchControllerTest.java (테스트 패턴 — getAdminToken(), MockMvc.perform() 패턴)
  </read_first>
  <behavior>
    - Test getNotifications_returns200: admin 토큰으로 GET /notifications → 200 + success:true + data.content 배열
    - Test getNotifications_unauthenticated_returns401: 토큰 없이 GET /notifications → 401
    - Test unreadCount_returns200: admin 토큰으로 GET /notifications/unread-count → 200 + success:true + data가 숫자
    - Test markRead_returns200: 알림 생성 후 PATCH /notifications/{id}/read → 200
    - Test markRead_otherUser_returns404: 다른 사용자의 알림 id로 PATCH /notifications/{id}/read → 404
    - Test markAllRead_returns200: PATCH /notifications/read-all → 200
  </behavior>
  <action>
    1. NotificationController를 com.company.wiki.notification.controller 패키지에 생성한다. @RestController, @RequestMapping("/notifications"), @RequiredArgsConstructor.
       - GET /: public ApiResponse&lt;NotificationService.PagedResponse&lt;NotificationDto.Response&gt;&gt; getNotifications(@RequestParam(required=false) Boolean isRead, Pageable pageable, @AuthenticationPrincipal UserDetails principal)
         → return ApiResponse.ok(notificationService.getNotifications(getCurrentUserId(principal), isRead, pageable));
       - GET /unread-count: public ApiResponse&lt;Long&gt; getUnreadCount(@AuthenticationPrincipal UserDetails principal)
         → return ApiResponse.ok(notificationService.getUnreadCount(getCurrentUserId(principal)));
       - PATCH /{id}/read: public ApiResponse&lt;Void&gt; markRead(@PathVariable Long id, @AuthenticationPrincipal UserDetails principal)
         → notificationService.markAsRead(id, getCurrentUserId(principal)); return ApiResponse.ok(null);
       - PATCH /read-all: public ApiResponse&lt;Void&gt; markAllRead(@AuthenticationPrincipal UserDetails principal)
         → notificationService.markAllAsRead(getCurrentUserId(principal)); return ApiResponse.ok(null);
       - private Long getCurrentUserId(UserDetails p) { return Long.parseLong(p.getUsername()); }

    2. NotificationControllerTest를 @SpringBootTest + @AutoConfigureMockMvc + @ActiveProfiles("local") + @Transactional으로 작성.
       - @Autowired NotificationRepository notificationRepository — 테스트 데이터 직접 생성
       - @BeforeEach: admin 토큰 획득 (POST /auth/login admin/Admin1234!)
       - markRead_otherUser_returns404: admin userId와 다른 userId로 Notification 직접 저장 후 admin 토큰으로 PATCH 시도 → 404
       - 나머지 시나리오 MockMvc로 검증
  </action>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@21 ./mvnw test -pl backend -Dtest=NotificationControllerTest -f /Users/shinwon/confluence-clone-proj/pom.xml</automated>
  </verify>
  <acceptance_criteria>
    - NotificationController.java에 @RestController @RequestMapping("/notifications") 어노테이션이 존재한다
    - NotificationController.java에 GET /, GET /unread-count, PATCH /{id}/read, PATCH /read-all 4개 엔드포인트가 존재한다
    - NotificationControllerTest에 markRead_otherUser_returns404 테스트가 존재한다
    - NotificationControllerTest 실행 결과 BUILD SUCCESS이고 모든 테스트가 GREEN이다
    - GET /notifications/unread-count 응답이 data 필드에 숫자 값을 포함한다
  </acceptance_criteria>
  <done>NotificationController 4개 엔드포인트 완성, NOTIF-02 통합 테스트 모두 GREEN</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → GET /notifications | JWT 인증 필수 — 미인증 시 401 |
| PATCH /notifications/{id}/read | IDOR 경계 — userId 소유자 검증 (NotificationService에서 처리) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Information Disclosure | GET /notifications | mitigate | JWT userId 기반 쿼리 — 다른 사용자 알림 조회 불가 |
| T-04-04-02 | Tampering | PATCH /notifications/{id}/read | mitigate | NotificationService.markAsRead()에서 findByIdAndUserId 소유자 검증 — 04-02 구현 |
| T-04-04-03 | Elevation of Privilege | /notifications 인증 | mitigate | Spring Security 전역 인증 필터 적용 — 토큰 없는 요청 401 |
| T-04-04-SC | Tampering | npm/pip/cargo installs | accept | 이 플랜은 신규 패키지 설치 없음 |
</threat_model>

<verification>
1. NotificationControllerTest 실행 — BUILD SUCCESS, 모든 테스트 GREEN
2. 전체 백엔드 테스트 실행 (Wave 2 완료 후): `JAVA_HOME=/opt/homebrew/opt/openjdk@21 ./mvnw test -pl backend -Dtest=AdminControllerTest,NotificationControllerTest,AuditLogServiceTest,NotificationServiceTest`
</verification>

<success_criteria>
- NotificationController 4개 엔드포인트 완성
- IDOR 방지 테스트(markRead_otherUser_returns404) GREEN
- Wave 2 전체(04-03 + 04-04) 테스트 모두 GREEN
</success_criteria>

<output>
Create `.planning/phases/04-admin/04-04-SUMMARY.md` when done
</output>
