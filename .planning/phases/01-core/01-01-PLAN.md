---
phase: 01-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/company/wiki/user/entity/Group.java
  - backend/src/main/java/com/company/wiki/user/entity/GroupMember.java
  - backend/src/main/java/com/company/wiki/user/repository/GroupRepository.java
  - backend/src/main/java/com/company/wiki/user/repository/GroupMemberRepository.java
  - backend/src/main/java/com/company/wiki/user/dto/UserDto.java
  - backend/src/main/java/com/company/wiki/user/dto/GroupDto.java
  - backend/src/main/java/com/company/wiki/user/service/UserService.java
  - backend/src/main/java/com/company/wiki/user/service/GroupService.java
  - backend/src/main/java/com/company/wiki/user/controller/UserController.java
  - backend/src/main/java/com/company/wiki/user/controller/GroupController.java
  - backend/src/main/java/com/company/wiki/common/exception/ErrorCode.java
  - backend/src/test/java/com/company/wiki/user/controller/UserControllerTest.java
  - backend/src/test/java/com/company/wiki/user/controller/GroupControllerTest.java
autonomous: true
requirements: [REQ-USER, REQ-GROUP]
must_haves:
  truths:
    - "POST /users 로 신규 사용자를 생성할 수 있다"
    - "GET /users/{id} 로 사용자 정보를 조회할 수 있다"
    - "PUT /users/{id} 로 사용자 정보를 수정할 수 있다"
    - "DELETE /users/{id} 로 사용자를 비활성화할 수 있다 (soft delete)"
    - "POST /groups 로 그룹을 생성하고 멤버를 추가할 수 있다"
    - "SITE_ADMIN 권한이어야 사용자/그룹 관리 API를 사용할 수 있다"
  artifacts:
    - path: backend/src/main/java/com/company/wiki/user/service/UserService.java
      provides: User CRUD 서비스 로직
      min_lines: 60
    - path: backend/src/main/java/com/company/wiki/user/controller/UserController.java
      provides: User REST 엔드포인트
      min_lines: 50
    - path: backend/src/main/java/com/company/wiki/user/controller/GroupController.java
      provides: Group REST 엔드포인트
      min_lines: 40
  key_links:
    - from: backend/src/main/java/com/company/wiki/user/controller/UserController.java
      to: backend/src/main/java/com/company/wiki/user/service/UserService.java
      via: 의존성 주입
      pattern: "UserService"
---

<objective>
사용자/그룹 CRUD API를 TDD로 구현한다.

Purpose: Phase 1 핵심 기능의 기반 — Space/콘텐츠 생성자, 권한 주체로 사용자/그룹이 필요하다.
Output: User CRUD, Group CRUD, 그룹 멤버 관리 API + 통합 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/src/main/java/com/company/wiki/user/entity/User.java
@backend/src/main/java/com/company/wiki/user/repository/UserRepository.java
@backend/src/main/java/com/company/wiki/user/controller/UserController.java
@backend/src/main/java/com/company/wiki/common/exception/ErrorCode.java
@backend/src/main/java/com/company/wiki/common/response/ApiResponse.java
@backend/src/main/java/com/company/wiki/common/config/SecurityConfig.java
@backend/src/main/resources/db/migration/V1__init_users_groups.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Group/GroupMember 엔티티 및 레포지토리 작성</name>
  <files>
    backend/src/main/java/com/company/wiki/user/entity/Group.java,
    backend/src/main/java/com/company/wiki/user/entity/GroupMember.java,
    backend/src/main/java/com/company/wiki/user/repository/GroupRepository.java,
    backend/src/main/java/com/company/wiki/user/repository/GroupMemberRepository.java
  </files>
  <read_first>
    backend/src/main/resources/db/migration/V1__init_users_groups.sql,
    backend/src/main/java/com/company/wiki/user/entity/User.java
  </read_first>
  <action>
V1 마이그레이션의 groups, group_members 테이블에 맞게 엔티티 작성.

Group.java:
- @Entity @Table(name = "groups")
- id(Long, IDENTITY), name(VARCHAR 200, UNIQUE, NOT NULL), description(TEXT), createdAt(TIMESTAMP, NOT NULL)
- @PrePersist: createdAt = LocalDateTime.now()
- @OneToMany(mappedBy="group", cascade=CascadeType.ALL) List<GroupMember> members

GroupMember.java:
- @Entity @Table(name = "group_members")
- @EmbeddedId GroupMemberId id (groupId Long, userId Long)
- @ManyToOne(fetch=LAZY) @MapsId("groupId") Group group
- @ManyToOne(fetch=LAZY) @MapsId("userId") User user
- GroupMemberId @Embeddable record: groupId Long, userId Long

GroupRepository: JpaRepository<Group, Long>
- Optional<Group> findByName(String name)
- boolean existsByName(String name)

GroupMemberRepository: JpaRepository<GroupMember, GroupMemberId>
- List<GroupMember> findByGroupId(Long groupId)
- List<GroupMember> findByUserId(Long userId)
- void deleteByGroupIdAndUserId(Long groupId, Long userId)
  </action>
  <verify>cd backend && export PATH="/opt/homebrew/opt/openjdk@21/bin:$PATH" && ./mvnw compile -q 2>&1 | tail -5</verify>
  <acceptance_criteria>
    - Group.java에 @Entity, @Table(name = "groups") 포함
    - GroupMember.java에 @EmbeddedId 포함
    - mvnw compile 성공 (BUILD SUCCESS)
  </acceptance_criteria>
  <done>컴파일 성공 + Group/GroupMember 엔티티 존재</done>
</task>

<task type="auto">
  <name>Task 2: UserDto/GroupDto 및 ErrorCode 보완</name>
  <files>
    backend/src/main/java/com/company/wiki/user/dto/UserDto.java,
    backend/src/main/java/com/company/wiki/user/dto/GroupDto.java,
    backend/src/main/java/com/company/wiki/common/exception/ErrorCode.java
  </files>
  <read_first>
    backend/src/main/java/com/company/wiki/common/exception/ErrorCode.java,
    backend/src/main/java/com/company/wiki/user/entity/User.java,
    backend/src/main/java/com/company/wiki/auth/dto/TokenResponse.java
  </read_first>
  <action>
UserDto.java (중첩 클래스로 record 사용):
- CreateRequest: loginId(NotBlank), name(NotBlank), email(Email, NotBlank), password(NotBlank, Size(min=8)), role(기본값 MEMBER)
- UpdateRequest: name, email, role (모두 Optional @Nullable)
- Response: id, loginId, name, email, role, status, createdAt
- static Response from(User user) 팩토리 메서드

GroupDto.java:
- CreateRequest: name(NotBlank), description
- Response: id, name, description, createdAt, memberCount

ErrorCode에 추가:
- GROUP_NOT_FOUND(HttpStatus.NOT_FOUND, "그룹을 찾을 수 없습니다.")
- DUPLICATE_EMAIL(HttpStatus.CONFLICT, "이미 사용 중인 이메일입니다.")
  </action>
  <verify>cd backend && export PATH="/opt/homebrew/opt/openjdk@21/bin:$PATH" && ./mvnw compile -q 2>&1 | tail -5</verify>
  <acceptance_criteria>
    - UserDto.java의 Response 클래스에 from(User) 메서드 존재
    - ErrorCode에 GROUP_NOT_FOUND 포함
    - 컴파일 성공
  </acceptance_criteria>
  <done>DTO 클래스 완성, 컴파일 성공</done>
</task>

<task type="auto">
  <name>Task 3: UserService/GroupService 구현</name>
  <files>
    backend/src/main/java/com/company/wiki/user/service/UserService.java,
    backend/src/main/java/com/company/wiki/user/service/GroupService.java
  </files>
  <read_first>
    backend/src/main/java/com/company/wiki/user/repository/UserRepository.java,
    backend/src/main/java/com/company/wiki/user/dto/UserDto.java,
    backend/src/main/java/com/company/wiki/common/exception/ErrorCode.java,
    backend/src/main/java/com/company/wiki/auth/service/AuthService.java
  </read_first>
  <action>
UserService.java (@Service, @Transactional):
- List<UserDto.Response> findAll()
- UserDto.Response findById(Long id)  [USER_NOT_FOUND 예외]
- UserDto.Response create(UserDto.CreateRequest req)
  → existsByLoginId 중복 체크(DUPLICATE_LOGIN_ID), existsByEmail(DUPLICATE_EMAIL)
  → passwordEncoder.encode(password)
  → User 저장 후 Response 반환
- UserDto.Response update(Long id, UserDto.UpdateRequest req) [USER_NOT_FOUND]
- void deactivate(Long id) [USER_NOT_FOUND, status = "INACTIVE" soft delete]
- UserDto.Response findMe(Long userId)

GroupService.java (@Service, @Transactional):
- List<GroupDto.Response> findAll()
- GroupDto.Response findById(Long id) [GROUP_NOT_FOUND]
- GroupDto.Response create(GroupDto.CreateRequest req) [existsByName → DUPLICATE_SPACE_KEY 재사용 대신 별도 처리]
- GroupDto.Response update(Long id, GroupDto.CreateRequest req)
- void delete(Long id)
- void addMember(Long groupId, Long userId)
- void removeMember(Long groupId, Long userId)
- List<UserDto.Response> getMembers(Long groupId)
  </action>
  <verify>cd backend && export PATH="/opt/homebrew/opt/openjdk@21/bin:$PATH" && ./mvnw compile -q 2>&1 | tail -5</verify>
  <acceptance_criteria>
    - UserService에 create, update, deactivate, findById 메서드 존재
    - GroupService에 addMember, removeMember 메서드 존재
    - 컴파일 성공
  </acceptance_criteria>
  <done>서비스 레이어 완성, 컴파일 성공</done>
</task>

<task type="auto">
  <name>Task 4: UserController/GroupController REST 엔드포인트 + SecurityConfig 수정</name>
  <files>
    backend/src/main/java/com/company/wiki/user/controller/UserController.java,
    backend/src/main/java/com/company/wiki/user/controller/GroupController.java,
    backend/src/main/java/com/company/wiki/common/config/SecurityConfig.java
  </files>
  <read_first>
    backend/src/main/java/com/company/wiki/user/controller/UserController.java,
    backend/src/main/java/com/company/wiki/common/config/SecurityConfig.java,
    backend/src/main/java/com/company/wiki/user/service/UserService.java
  </read_first>
  <action>
UserController.java (기존 파일 수정):
- GET /users → 전체 목록 (SITE_ADMIN)
- GET /users/{id} → 단건 조회 (SITE_ADMIN 또는 본인)
- POST /users → 생성 (SITE_ADMIN)
- PUT /users/{id} → 수정 (SITE_ADMIN)
- DELETE /users/{id} → 비활성화 (SITE_ADMIN)
- GET /users/me → 현재 사용자 정보 (기존 메서드 유지, UserService.findMe 호출로 리팩토링)

GroupController.java (새 파일):
- @RestController @RequestMapping("/groups")
- GET /groups → 목록
- GET /groups/{id} → 단건
- POST /groups → 생성 (SITE_ADMIN)
- PUT /groups/{id} → 수정 (SITE_ADMIN)
- DELETE /groups/{id} → 삭제 (SITE_ADMIN)
- POST /groups/{id}/members → 멤버 추가 (body: { userId: Long })
- DELETE /groups/{id}/members/{userId} → 멤버 제거
- GET /groups/{id}/members → 멤버 목록

SecurityConfig.java 수정:
- /users/me → authenticated (기존 anyRequest 커버)
- /users/**, /groups/** → authenticated (SITE_ADMIN 체크는 @PreAuthorize 또는 서비스 레이어에서)
- @EnableMethodSecurity 추가 (메서드 레벨 권한 체크 허용)
  </action>
  <verify>cd backend && export PATH="/opt/homebrew/opt/openjdk@21/bin:$PATH" && ./mvnw compile -q 2>&1 | tail -5</verify>
  <acceptance_criteria>
    - UserController에 GET /users, POST /users, PUT /users/{id}, DELETE /users/{id} 매핑 존재
    - GroupController에 /groups 매핑 존재
    - SecurityConfig에 @EnableMethodSecurity 또는 hasRole 설정 포함
    - 컴파일 성공
  </acceptance_criteria>
  <done>컨트롤러 완성, 컴파일 성공</done>
</task>

<task type="auto">
  <name>Task 5: TDD — 사용자/그룹 API 통합 테스트 작성 및 통과 확인</name>
  <files>
    backend/src/test/java/com/company/wiki/user/controller/UserControllerTest.java,
    backend/src/test/java/com/company/wiki/user/controller/GroupControllerTest.java
  </files>
  <read_first>
    backend/src/test/java/com/company/wiki/auth/controller/AuthControllerTest.java,
    backend/src/main/java/com/company/wiki/user/dto/UserDto.java
  </read_first>
  <action>
UserControllerTest.java (@SpringBootTest, @AutoConfigureMockMvc, @ActiveProfiles("local"), @Transactional):
1. admin 토큰 획득 헬퍼 메서드 작성
   → POST /auth/login { loginId: "admin", password: "Admin1234!" } → accessToken 추출
2. createUser_asAdmin_returns200(): POST /users { loginId: "testuser", name: "테스트", email: "test@co.com", password: "Pass1234!" }
   → 200 OK, data.id 존재
3. getUser_withoutToken_returns401(): GET /users/1 → 401

GroupControllerTest.java:
1. createGroup_asAdmin_returns200(): POST /groups { name: "개발팀" } → 200 OK
2. addMember_asAdmin_returns200(): 그룹 생성 → 사용자 생성 → POST /groups/{id}/members { userId } → 200 OK

테스트 실행:
cd backend && export PATH="/opt/homebrew/opt/openjdk@21/bin:$PATH" && ./mvnw test -Dtest="UserControllerTest,GroupControllerTest" -Dspring.profiles.active=local 2>&1 | tail -20
  </action>
  <verify>cd backend && export PATH="/opt/homebrew/opt/openjdk@21/bin:$PATH" && ./mvnw test -Dtest="UserControllerTest,GroupControllerTest" -Dspring.profiles.active=local 2>&1 | tail -10</verify>
  <acceptance_criteria>
    - Tests run: 5 이상
    - BUILD SUCCESS
    - Failures: 0
  </acceptance_criteria>
  <done>모든 테스트 통과, BUILD SUCCESS</done>
</task>

</tasks>

<verification>
- [ ] cd backend && export PATH="/opt/homebrew/opt/openjdk@21/bin:$PATH" && ./mvnw test -Dtest="UserControllerTest,GroupControllerTest" -Dspring.profiles.active=local 2>&1 | grep -E "Tests run|BUILD"
- [ ] GET /users/me (인증 필요) 동작 확인
- [ ] Group 멤버 추가/제거 동작 확인
</verification>

<success_criteria>
- 모든 테스트 통과 (BUILD SUCCESS)
- User CRUD API 동작
- Group CRUD + 멤버 관리 API 동작
- 미인증 요청은 401 반환
</success_criteria>

<output>
After completion, create `.planning/phases/01-core/01-01-SUMMARY.md`
</output>
