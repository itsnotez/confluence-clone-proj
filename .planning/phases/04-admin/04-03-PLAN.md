---
phase: 04-admin
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - backend/src/main/java/com/company/wiki/admin/dto/AdminStatsDto.java
  - backend/src/main/java/com/company/wiki/admin/service/AdminStatsService.java
  - backend/src/main/java/com/company/wiki/admin/controller/AdminController.java
  - backend/src/main/java/com/company/wiki/attachment/repository/AttachmentRepository.java
  - backend/src/test/java/com/company/wiki/admin/controller/AdminControllerTest.java
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-02
  - AUDIT-02

must_haves:
  truths:
    - "SITE_ADMIN 토큰으로 GET /admin/stats 호출 시 200과 activeUsers/totalSpaces/totalContents/storageUsedBytes 필드를 포함한 JSON을 반환한다"
    - "MEMBER 역할 토큰으로 GET /admin/stats 호출 시 403을 반환한다"
    - "GET /admin/audit-logs?actionType=SPACE_DELETE는 SPACE_DELETE 레코드만 반환하고 페이징 메타(totalElements, totalPages)를 포함한다"
    - "GET /admin/audit-logs는 인증 없이 접근 시 401을 반환한다"
    - "Site Admin이 /admin/** 엔드포인트 접근 시 audit_logs에 is_admin_access=true ADMIN_ACCESS 레코드가 기록된다"
  artifacts:
    - path: "backend/src/main/java/com/company/wiki/admin/controller/AdminController.java"
      provides: "GET /admin/stats, GET /admin/audit-logs 엔드포인트"
      contains: "class AdminController"
    - path: "backend/src/main/java/com/company/wiki/admin/service/AdminStatsService.java"
      provides: "통계 집계 서비스 (COUNT + SUM)"
      contains: "getStats()"
    - path: "backend/src/test/java/com/company/wiki/admin/controller/AdminControllerTest.java"
      provides: "ADMIN-01, ADMIN-02, AUDIT-02 통합 테스트"
      contains: "stats_siteAdmin_returns200"
  key_links:
    - from: "AdminController.getStats()"
      to: "AdminStatsService.getStats()"
      via: "직접 호출"
    - from: "AdminController.getAuditLogs()"
      to: "AuditLogService.findByFilter()"
      via: "직접 호출 — Wave 1(04-01) AuditLogService 사용"
    - from: "AdminStatsService"
      to: "AttachmentRepository.sumSizeBytes()"
      via: "@Query JPQL — attachments.size_bytes SUM"
---

<objective>
관리자 통계 API와 감사로그 조회 API를 구현한다: AdminStatsDto/AdminStatsService/AdminController를 신규 생성하고, Wave 1에서 생성된 AuditLogService.findByFilter()를 AdminController에 연결한다.

Purpose: Site Admin이 운영 현황(사용자/Space/콘텐츠 수, 스토리지 사용량, 메일 상태)을 단일 API로 조회하고, 감사로그를 필터/페이징으로 열람할 수 있게 한다.

Output: AdminStatsDto, AdminStatsService, AdminController, AttachmentRepository sumSizeBytes 추가, AdminControllerTest
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-admin/04-CONTEXT.md
@.planning/phases/04-admin/04-RESEARCH.md
@.planning/phases/04-admin/04-01-SUMMARY.md

<interfaces>
<!-- AuditLogService (04-01에서 생성) -->
<!--
  PagedResponse<AuditLogDto.Response> findByFilter(Long actorId, String actionType,
      LocalDateTime from, LocalDateTime to, Pageable pageable)
  record PagedResponse<T>(List<T> content, int page, int size, long totalElements, int totalPages)
-->

<!-- SecurityConfig — /admin/** 이미 hasRole("SITE_ADMIN") 보호 중 (추가 @PreAuthorize 불필요) -->

<!-- SpaceController 컨트롤러 패턴 (검증됨) -->
<!--
  @RestController @RequestMapping("/admin") @RequiredArgsConstructor
  return ApiResponse.ok(service.method());
  @AuthenticationPrincipal UserDetails principal
  private Long getCurrentUserId(UserDetails p) { return Long.parseLong(p.getUsername()); }
-->

<!-- AttachmentRepository 현재 상태 (검증됨) -->
<!--
  findByContentId(Long contentId) 이미 존재
  sumSizeBytes() @Query 추가 필요: @Query("SELECT COALESCE(SUM(a.sizeBytes), 0) FROM Attachment a")
-->

<!-- MailAccount sync_status 값 — "ACTIVE" / "DISABLED" (MailAccount 엔티티 확인 필요) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AdminStatsDto·AdminStatsService·AttachmentRepository sumSizeBytes 생성</name>
  <files>
    backend/src/main/java/com/company/wiki/admin/dto/AdminStatsDto.java,
    backend/src/main/java/com/company/wiki/admin/service/AdminStatsService.java,
    backend/src/main/java/com/company/wiki/attachment/repository/AttachmentRepository.java
  </files>
  <read_first>
    - backend/src/main/java/com/company/wiki/attachment/repository/AttachmentRepository.java (기존 메서드 확인 — sumSizeBytes 추가 위치)
    - backend/src/main/java/com/company/wiki/user/repository/UserRepository.java (countByStatus 또는 countBy 메서드 존재 여부 확인)
    - backend/src/main/java/com/company/wiki/space/repository/SpaceRepository.java (countByDeletedAtIsNull 존재 여부 확인)
    - backend/src/main/java/com/company/wiki/content/repository/ContentRepository.java (countByDeletedAtIsNull 존재 여부 확인)
    - backend/src/main/java/com/company/wiki/mail/repository/MailAccountRepository.java (countBySyncStatus 존재 여부 확인)
  </read_first>
  <behavior>
    - Test 1: AdminStatsService.getStats()가 반환하는 AdminStatsDto에 activeUsers 필드가 Long 타입으로 존재한다
    - Test 2: AdminStatsService.getStats()가 반환하는 AdminStatsDto에 storageUsedBytes 필드가 0 이상의 값이다
    - Test 3: AdminStatsService.getStats()가 반환하는 AdminStatsDto에 totalSpaces, totalContents, mailAccountsOk, mailAccountsFailed 필드가 모두 존재한다
  </behavior>
  <action>
    1. AdminStatsDto를 com.company.wiki.admin.dto 패키지에 생성한다. @Builder record 또는 @Builder @Getter @NoArgsConstructor @AllArgsConstructor class 사용. 필드: activeUsers (long), totalSpaces (long), totalContents (long), storageUsedBytes (long), mailAccountsOk (long), mailAccountsFailed (long).

    2. AttachmentRepository에 sumSizeBytes() 추가:
       @Query("SELECT COALESCE(SUM(a.sizeBytes), 0) FROM Attachment a")
       long sumSizeBytes();
       Attachment 엔티티의 size_bytes 컬럼이 매핑된 필드명을 읽은 후 정확히 사용한다 (sizeBytes 또는 다른 이름일 수 있음).

    3. AdminStatsService를 com.company.wiki.admin.service 패키지에 생성한다. @Service, @RequiredArgsConstructor, @Transactional(readOnly=true). getStats() 메서드에서:
       - long activeUsers = userRepository.countByStatus("ACTIVE"); — UserRepository에 해당 메서드 없으면 @Query("SELECT COUNT(u) FROM User u WHERE u.status = 'ACTIVE'") long countActiveUsers(); 추가
       - long totalSpaces = spaceRepository.countByDeletedAtIsNull(); — 없으면 @Query("SELECT COUNT(s) FROM Space s WHERE s.deletedAt IS NULL") long countActiveSpaces(); 추가
       - long totalContents = contentRepository.countByDeletedAtIsNull(); — 없으면 동일 패턴으로 추가
       - long storageUsedBytes = attachmentRepository.sumSizeBytes();
       - long mailAccountsOk = mailAccountRepository.countBySyncStatus("ACTIVE"); — 없으면 추가
       - long mailAccountsFailed = mailAccountRepository.countBySyncStatus("DISABLED"); — 없으면 추가
       - AdminStatsDto.builder()로 조립하여 반환
  </action>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@21 ./mvnw compile -pl backend -f /Users/shinwon/confluence-clone-proj/pom.xml</automated>
  </verify>
  <acceptance_criteria>
    - AdminStatsDto.java에 activeUsers, totalSpaces, totalContents, storageUsedBytes, mailAccountsOk, mailAccountsFailed 필드가 모두 존재한다
    - AttachmentRepository.java에 COALESCE(SUM 패턴의 @Query가 존재한다
    - AdminStatsService.java에 getStats() 메서드가 존재하고 @Transactional(readOnly=true)가 적용되어 있다
    - mvnw compile이 BUILD SUCCESS로 완료된다
  </acceptance_criteria>
  <done>AdminStatsDto, AdminStatsService, AttachmentRepository sumSizeBytes 생성 완료, 컴파일 성공</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AdminController 생성 + 통합 테스트</name>
  <files>
    backend/src/main/java/com/company/wiki/admin/controller/AdminController.java,
    backend/src/test/java/com/company/wiki/admin/controller/AdminControllerTest.java
  </files>
  <read_first>
    - backend/src/main/java/com/company/wiki/space/controller/SpaceController.java (컨트롤러 패턴 — @RestController, ApiResponse, @AuthenticationPrincipal 사용법)
    - backend/src/main/java/com/company/wiki/admin/service/AdminStatsService.java (Task 1에서 생성한 getStats() 확인)
    - backend/src/main/java/com/company/wiki/auditlog/service/AuditLogService.java (04-01에서 생성한 findByFilter() 시그니처 확인)
    - backend/src/test/java/com/company/wiki/search/SearchControllerTest.java (테스트 패턴 — @SpringBootTest, getAdminToken() 패턴 참조)
  </read_first>
  <behavior>
    - Test stats_siteAdmin_returns200: admin 토큰으로 GET /admin/stats → 200 + success:true + data.activeUsers 존재
    - Test stats_member_returns403: MEMBER 토큰으로 GET /admin/stats → 403
    - Test stats_unauthenticated_returns401: 토큰 없이 GET /admin/stats → 401
    - Test auditLogs_returns200: admin 토큰으로 GET /admin/audit-logs → 200 + success:true + data.content 배열 존재
    - Test auditLogs_filterByActionType: GET /admin/audit-logs?actionType=SPACE_DELETE → 응답 data.content의 모든 항목 actionType == "SPACE_DELETE"
  </behavior>
  <action>
    1. AdminController를 com.company.wiki.admin.controller 패키지에 생성한다. @RestController, @RequestMapping("/admin"), @RequiredArgsConstructor.
       - GET /stats: public ApiResponse&lt;AdminStatsDto&gt; getStats(@AuthenticationPrincipal UserDetails principal) { Long actorId = Long.parseLong(principal.getUsername()); try { auditLogService.record(actorId, "ADMIN_ACCESS", "ADMIN", null, Map.of("endpoint", "GET /admin/stats"), true); } catch (Exception e) { log.warn("audit failed: {}", e.getMessage()); } return ApiResponse.ok(adminStatsService.getStats()); }
       - GET /audit-logs: public ApiResponse&lt;PagedResponse&lt;AuditLogDto.Response&gt;&gt; getAuditLogs(@AuthenticationPrincipal UserDetails principal, @RequestParam(required=false) Long actorId, @RequestParam(required=false) String actionType, @RequestParam(required=false) @DateTimeFormat(iso=DateTimeFormat.ISO.DATE_TIME) LocalDateTime from, @RequestParam(required=false) @DateTimeFormat(iso=DateTimeFormat.ISO.DATE_TIME) LocalDateTime to, Pageable pageable) { Long userId = Long.parseLong(principal.getUsername()); try { auditLogService.record(userId, "ADMIN_ACCESS", "ADMIN", null, Map.of("endpoint", "GET /admin/audit-logs"), true); } catch (Exception e) { log.warn("audit failed: {}", e.getMessage()); } return ApiResponse.ok(auditLogService.findByFilter(actorId, actionType, from, to, pageable)); }
       주의: /admin/** 경로는 SecurityConfig에서 이미 ROLE_SITE_ADMIN으로 보호됨 — @PreAuthorize 추가 불필요.

    2. AdminControllerTest를 @SpringBootTest + @AutoConfigureMockMvc + @ActiveProfiles("local") + @Transactional으로 작성.
       - getAdminToken(): POST /auth/login (admin / Admin1234!) 후 accessToken 추출
       - getMemberToken(): 일반 사용자 계정 생성 또는 기존 테스트 사용자 활용
       - 위 5개 시나리오 각각 MockMvc.perform()으로 검증
       - GET /admin/stats 응답: $.success == true, $.data.activeUsers가 존재함
       - GET /admin/audit-logs 응답: $.success == true, $.data.content가 배열임
  </action>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@21 ./mvnw test -pl backend -Dtest=AdminControllerTest -f /Users/shinwon/confluence-clone-proj/pom.xml</automated>
  </verify>
  <acceptance_criteria>
    - AdminController.java에 @RestController @RequestMapping("/admin") 어노테이션이 존재한다
    - AdminController.java에 GET /stats와 GET /audit-logs 두 엔드포인트가 존재한다
    - AdminControllerTest에 stats_siteAdmin_returns200, stats_member_returns403 테스트가 존재한다
    - AdminControllerTest 실행 결과 BUILD SUCCESS이고 모든 테스트가 GREEN이다
    - GET /admin/stats가 403을 MEMBER 역할에 반환하는 테스트가 통과된다
  </acceptance_criteria>
  <done>AdminController 생성 완료, AdminControllerTest 모든 시나리오 GREEN</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /admin/** | SecurityConfig에서 ROLE_SITE_ADMIN 강제 — 이 경계를 우회하는 구현 금지 |
| GET /admin/audit-logs 쿼리 파라미터 | actorId/actionType/from/to — @RequestParam nullable, 타입 검증 Spring이 자동 처리 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Elevation of Privilege | GET /admin/stats | mitigate | SecurityConfig `/admin/**` hasRole("SITE_ADMIN") — 이미 적용됨; @PreAuthorize 중복 불필요 |
| T-04-03-02 | Information Disclosure | GET /admin/audit-logs | mitigate | 동일 — /admin/** 보호 범위에 포함 |
| T-04-03-03 | Tampering | audit_logs 조회 JPQL | mitigate | @Param 바인딩 사용 — SQL 인젝션 불가 |
| T-04-03-04 | Information Disclosure | AdminStatsDto storageUsedBytes | accept | 사이트 관리자 전용 정보 — SITE_ADMIN에게만 노출, 낮은 위험 |
| T-04-03-SC | Tampering | npm/pip/cargo installs | accept | 이 플랜은 신규 패키지 설치 없음 |
</threat_model>

<verification>
1. AdminControllerTest 실행 — BUILD SUCCESS, 모든 테스트 GREEN
2. SITE_ADMIN 토큰으로 직접 API 호출 확인 (선택):
   curl -H "Authorization: Bearer {token}" http://localhost:8080/api/admin/stats
   → {"success":true,"data":{"activeUsers":...},...}
3. 전체 백엔드 테스트 실행: `JAVA_HOME=/opt/homebrew/opt/openjdk@21 ./mvnw test -pl backend`
</verification>

<success_criteria>
- AdminController의 GET /admin/stats와 GET /admin/audit-logs 엔드포인트 완성
- SITE_ADMIN 전용 접근 제어 동작 확인 (403 테스트 GREEN)
- 감사로그 필터(actionType, 기간, actor) 동적 쿼리 동작
- AdminControllerTest 모든 시나리오 GREEN
</success_criteria>

<output>
Create `.planning/phases/04-admin/04-03-SUMMARY.md` when done
</output>
