---
phase: 02-mail
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - backend/src/main/java/com/company/wiki/mail/entity/MailMessage.java
  - backend/src/main/java/com/company/wiki/mail/repository/MailMessageRepository.java
  - backend/src/main/java/com/company/wiki/mail/service/ImapService.java
  - backend/src/main/java/com/company/wiki/mail/service/MailSyncService.java
  - backend/src/main/java/com/company/wiki/mail/scheduler/MailPollingScheduler.java
  - backend/src/test/java/com/company/wiki/mail/service/MailSyncServiceTest.java
autonomous: true
requirements: [REQ-MAIL-SYNC]
must_haves:
  truths:
    - "ImapService.fetchNewMessages()가 JavaMail로 IMAP 메시지를 fetch한다"
    - "MailSyncService가 message_uid 기준 중복을 방지하여 저장한다"
    - "MailPollingScheduler가 5분 주기로 syncAll()을 실행한다"
    - "동기화 3회 연속 실패 시 syncStatus가 DISABLED로 변경된다"
  artifacts:
    - path: backend/src/main/java/com/company/wiki/mail/service/ImapService.java
      provides: JavaMail IMAP 연결 및 메시지 fetch
      min_lines: 60
    - path: backend/src/main/java/com/company/wiki/mail/service/MailSyncService.java
      provides: 동기화 + 재시도 로직
      min_lines: 70
---

<objective>
IMAP Polling 기반 메일 동기화 서비스를 구현한다.

Purpose: 5분마다 등록된 IMAP 계정에서 신규 메일을 가져와 DB에 저장.
Output: MailMessage 엔티티, ImapService, MailSyncService, MailPollingScheduler
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-mail/02-01-SUMMARY.md
@backend/src/main/resources/db/migration/V5__mail.sql
@backend/src/main/java/com/company/wiki/mail/entity/MailAccount.java
@backend/src/main/java/com/company/wiki/mail/repository/MailAccountRepository.java
@backend/src/main/java/com/company/wiki/common/util/AesEncryptUtil.java
@backend/pom.xml
</context>

<tasks>

<task type="auto">
  <name>Task 1: MailMessage 엔티티 + 레포지토리</name>
  <files>
    backend/src/main/java/com/company/wiki/mail/entity/MailMessage.java,
    backend/src/main/java/com/company/wiki/mail/repository/MailMessageRepository.java
  </files>
  <read_first>backend/src/main/resources/db/migration/V5__mail.sql</read_first>
  <action>
MailMessage.java (@Entity @Table(name="mail_messages", uniqueConstraints=@UniqueConstraint(columnNames={"mail_account_id","message_uid"})) @Getter @Setter @NoArgsConstructor @Builder @AllArgsConstructor):
- id(Long IDENTITY), mailAccountId(Long @Column(name="mail_account_id") NOT NULL)
- messageUid(VARCHAR 500 @Column(name="message_uid") NOT NULL)
- threadId(VARCHAR 500), subject(VARCHAR 1000), sender(VARCHAR 500), recipients(TEXT)
- receivedAt(LocalDateTime), bodyText(TEXT), bodyHtml(TEXT)
- status(VARCHAR 20 기본 "UNREAD"), linkedContentId(Long nullable), createdAt(LocalDateTime)
- @PrePersist: createdAt=now(), status 기본값

MailMessageRepository: JpaRepository<MailMessage, Long>
- boolean existsByMailAccountIdAndMessageUid(Long mailAccountId, String messageUid)
- List<MailMessage> findByMailAccountIdOrderByReceivedAtDesc(Long mailAccountId)
- Optional<MailMessage> findByIdAndMailAccountId(Long id, Long mailAccountId)
  </action>
  <verify>cd backend && export JAVA_HOME="/opt/homebrew/opt/openjdk@21" && export PATH="$JAVA_HOME/bin:$PATH" && ./mvnw compile -q 2>&1 | tail -5</verify>
  <acceptance_criteria>
    - MailMessage.java에 messageUid, bodyText, status 필드 존재
    - 컴파일 성공
  </acceptance_criteria>
  <done>MailMessage 엔티티 컴파일 성공</done>
</task>

<task type="auto">
  <name>Task 2: ImapService — JavaMail IMAP fetch</name>
  <files>backend/src/main/java/com/company/wiki/mail/service/ImapService.java</files>
  <read_first>
    backend/src/main/java/com/company/wiki/mail/entity/MailAccount.java,
    backend/src/main/java/com/company/wiki/common/util/AesEncryptUtil.java,
    backend/pom.xml
  </read_first>
  <action>
pom.xml 확인: spring-boot-starter-mail 의존성 이미 있음(Phase 0에서 추가됨). 없으면 추가.

ImapService.java (@Service @RequiredArgsConstructor @Slf4j):
import: jakarta.mail.*, jakarta.mail.internet.MimeMessage, jakarta.mail.internet.MimeMultipart

List<MailMessage> fetchNewMessages(MailAccount account, int maxFetch):
  1. String password = aesEncryptUtil.decrypt(account.getCredential())
  2. Properties props = new Properties()
     props.put("mail.store.protocol", "imap")
     props.put("mail.imap.host", account.getImapHost())
     props.put("mail.imap.port", String.valueOf(account.getImapPort()))
     props.put("mail.imap.ssl.enable", String.valueOf(account.isImapSsl()))
     props.put("mail.imap.connectiontimeout", "5000")
     props.put("mail.imap.timeout", "5000")
  3. Session session = Session.getInstance(props)
     Store store = session.getStore("imap")  (또는 "imaps" if ssl)
     store.connect(account.getImapHost(), account.getEmailAddress(), password)
  4. Folder inbox = store.getFolder("INBOX")
     inbox.open(Folder.READ_ONLY)
  5. Message[] messages = inbox.getMessages()
     int start = Math.max(1, messages.length - maxFetch + 1)
     Message[] recent = inbox.getMessages(start, messages.length)
  6. List<MailMessage> result = new ArrayList<>()
     for (Message msg : recent):
       String uid = String.valueOf(((UIDFolder) inbox).getUID(msg))
       String subject = msg.getSubject() != null ? msg.getSubject() : "(제목없음)"
       String from = msg.getFrom() != null ? msg.getFrom()[0].toString() : ""
       LocalDateTime received = msg.getReceivedDate() != null ?
         msg.getReceivedDate().toInstant().atZone(ZoneId.systemDefault()).toLocalDateTime() : LocalDateTime.now()
       String body = extractText(msg)
       result.add(MailMessage.builder()
         .mailAccountId(account.getId()).messageUid(uid)
         .subject(subject).sender(from).receivedAt(received).bodyText(body).build())
  7. inbox.close(false); store.close()
  8. return result
  catch (MessagingException | IOException e): log.error, throw new RuntimeException(e)

private String extractText(Part part) throws MessagingException, IOException:
  if (part.isMimeType("text/plain")): return (String) part.getContent()
  if (part.isMimeType("multipart/*")):
    Multipart mp = (Multipart) part.getContent()
    for each: result += extractText(part)
  return ""
  </action>
  <verify>cd backend && export JAVA_HOME="/opt/homebrew/opt/openjdk@21" && export PATH="$JAVA_HOME/bin:$PATH" && ./mvnw compile -q 2>&1 | tail -5</verify>
  <acceptance_criteria>
    - ImapService.java에 fetchNewMessages 메서드 존재
    - jakarta.mail import 존재
    - 컴파일 성공
  </acceptance_criteria>
  <done>ImapService 컴파일 성공</done>
</task>

<task type="auto">
  <name>Task 3: MailSyncService + MailPollingScheduler</name>
  <files>
    backend/src/main/java/com/company/wiki/mail/service/MailSyncService.java,
    backend/src/main/java/com/company/wiki/mail/scheduler/MailPollingScheduler.java
  </files>
  <read_first>
    backend/src/main/java/com/company/wiki/mail/service/ImapService.java,
    backend/src/main/java/com/company/wiki/mail/repository/MailAccountRepository.java,
    backend/src/main/java/com/company/wiki/mail/repository/MailMessageRepository.java,
    backend/src/main/java/com/company/wiki/mail/service/MailAccountService.java
  </read_first>
  <action>
MailSyncService.java (@Service @RequiredArgsConstructor @Slf4j):

@Transactional
void syncAccount(MailAccount account):
  if ("DISABLED".equals(account.getSyncStatus())): return
  int retries = 0
  while (retries < 3):
    try:
      List<MailMessage> fetched = imapService.fetchNewMessages(account, 50)
      int saved = 0
      for (MailMessage msg : fetched):
        if (!mailMessageRepository.existsByMailAccountIdAndMessageUid(account.getId(), msg.getMessageUid())):
          mailMessageRepository.save(msg)
          saved++
      account.setSyncStatus("ACTIVE")
      account.setLastSyncedAt(LocalDateTime.now())
      mailAccountRepository.save(account)
      log.info("계정 {} 동기화 완료: {}건 저장", account.getEmailAddress(), saved)
      return
    catch (Exception e):
      retries++
      log.error("동기화 실패 ({}/3): {}", retries, e.getMessage())
      if (retries >= 3): account.setSyncStatus("DISABLED")
      else: account.setSyncStatus("ERROR")
      mailAccountRepository.save(account)

void syncAll():
  List<MailAccount> targets = new ArrayList<>()
  targets.addAll(mailAccountRepository.findBySyncStatus("ACTIVE"))
  targets.addAll(mailAccountRepository.findBySyncStatus("PENDING"))
  targets.addAll(mailAccountRepository.findBySyncStatus("ERROR"))
  targets.forEach(this::syncAccount)

MailPollingScheduler.java (@Component @RequiredArgsConstructor @Slf4j):
- @Scheduled(fixedDelay = 300000) void pollAll():
  log.info("메일 폴링 시작")
  mailSyncService.syncAll()
  log.info("메일 폴링 완료")
  </action>
  <verify>cd backend && export JAVA_HOME="/opt/homebrew/opt/openjdk@21" && export PATH="$JAVA_HOME/bin:$PATH" && ./mvnw compile -q 2>&1 | tail -5</verify>
  <acceptance_criteria>
    - MailSyncService에 syncAccount, syncAll 존재
    - MailPollingScheduler에 @Scheduled(fixedDelay=300000) 존재
    - 컴파일 성공
  </acceptance_criteria>
  <done>MailSyncService + 스케줄러 컴파일 성공</done>
</task>

<task type="auto">
  <name>Task 4: MailSyncService 단위 테스트 (Mockito)</name>
  <files>backend/src/test/java/com/company/wiki/mail/service/MailSyncServiceTest.java</files>
  <read_first>
    backend/src/test/java/com/company/wiki/permission/service/PermissionServiceTest.java,
    backend/src/main/java/com/company/wiki/mail/service/MailSyncService.java
  </read_first>
  <action>
MailSyncServiceTest.java (@ExtendWith(MockitoExtension.class)):
@Mock ImapService imapService
@Mock MailAccountRepository mailAccountRepository
@Mock MailMessageRepository mailMessageRepository
@InjectMocks MailSyncService mailSyncService

헬퍼: MailAccount testAccount() — id=1L, emailAddress="t@t.com", syncStatus="ACTIVE", credential="enc"

Test 1 — syncAccount_newMessages_savedSuccessfully():
  given: imapService.fetchNewMessages → [msg1(uid="1"), msg2(uid="2")]
         existsByMailAccountIdAndMessageUid(1L,"1") → false
         existsByMailAccountIdAndMessageUid(1L,"2") → false
  when: mailSyncService.syncAccount(account)
  then: verify(mailMessageRepository, times(2)).save(any())
        assertThat(account.getSyncStatus()).isEqualTo("ACTIVE")

Test 2 — syncAccount_duplicateMessage_skipped():
  given: fetchNewMessages → [msg1(uid="1")]
         existsByMailAccountIdAndMessageUid(1L,"1") → true
  when: syncAccount(account)
  then: verify(mailMessageRepository, never()).save(any(MailMessage.class))

Test 3 — syncAccount_imapFailure_3retries_disabled():
  given: fetchNewMessages → throws RuntimeException("IMAP error") (항상)
  when: syncAccount(account)
  then: assertThat(account.getSyncStatus()).isEqualTo("DISABLED")
        verify(mailAccountRepository, times(3)).save(account)  // 3회 재시도 저장

실행: cd backend && export JAVA_HOME="/opt/homebrew/opt/openjdk@21" && export PATH="$JAVA_HOME/bin:$PATH" && ./mvnw test -Dtest=MailSyncServiceTest 2>&1 | tail -10
  </action>
  <verify>cd backend && export JAVA_HOME="/opt/homebrew/opt/openjdk@21" && export PATH="$JAVA_HOME/bin:$PATH" && ./mvnw test -Dtest=MailSyncServiceTest 2>&1 | tail -10</verify>
  <acceptance_criteria>
    - Tests run: 3, BUILD SUCCESS, Failures: 0
  </acceptance_criteria>
  <done>MailSyncServiceTest 모든 테스트 통과</done>
</task>

</tasks>

<verification>
- [ ] ./mvnw test -Dtest=MailSyncServiceTest — BUILD SUCCESS
- [ ] 3회 실패 → DISABLED 확인
</verification>

<success_criteria>
- MailSyncServiceTest 3개 테스트 통과
- MailPollingScheduler 컴파일 성공
</success_criteria>

<output>
After completion, create `.planning/phases/02-mail/02-02-SUMMARY.md`
</output>
