---
phase: 01-core
plan: 07
type: execute
wave: 3
depends_on: ["01-04", "01-05", "01-06"]
files_modified:
  - frontend/src/components/content/VersionHistoryPanel.vue
  - frontend/src/views/content/ContentView.vue
  - frontend/src/api/permission.js
  - frontend/src/views/space/SpacePermissionView.vue
  - frontend/src/router/index.js
autonomous: true
requirements: [REQ-VERSION, REQ-PERMISSION, REQ-FRONTEND]
must_haves:
  truths:
    - "ContentView에서 '버전 기록' 버튼 클릭시 VersionHistoryPanel이 슬라이드인된다"
    - "VersionHistoryPanel에서 버전 목록을 조회할 수 있다"
    - "SpacePermissionView에서 Space 권한 목록을 조회하고 추가할 수 있다"
    - "SpacePermissionView 라우트가 /spaces/:key/permissions 로 접근 가능하다"
  artifacts:
    - path: frontend/src/components/content/VersionHistoryPanel.vue
      provides: 버전 기록 슬라이드 패널
      min_lines: 50
    - path: frontend/src/views/space/SpacePermissionView.vue
      provides: Space 권한 관리 화면
      min_lines: 60
    - path: frontend/src/api/permission.js
      provides: 권한 API 클라이언트
      min_lines: 15
  key_links:
    - from: frontend/src/views/content/ContentView.vue
      to: frontend/src/components/content/VersionHistoryPanel.vue
      via: v-model:visible로 패널 표시 제어
      pattern: "VersionHistoryPanel"
---

<objective>
버전 관리 패널(VersionHistoryPanel)과 Space 권한 관리 화면(SpacePermissionView)을 구현한다.

Purpose: 콘텐츠 버전 이력 조회와 Space 권한 관리 UI — Phase 1의 마지막 UI 요소.
Output: VersionHistoryPanel, SpacePermissionView, Permission API
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core/01-04-SUMMARY.md
@.planning/phases/01-core/01-06-SUMMARY.md
@frontend/src/views/content/ContentView.vue
@frontend/src/stores/content.js
@frontend/src/api/content.js
@frontend/src/router/index.js
@frontend/src/components/layout/AppHeader.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Permission API 클라이언트</name>
  <files>
    frontend/src/api/permission.js
  </files>
  <read_first>
    frontend/src/api/space.js
  </read_first>
  <action>
frontend/src/api/permission.js:
```js
import api from './axios'
export default {
  getSpacePermissions: (spaceKey) => api.get(`/spaces/${spaceKey}/permissions`),
  grantPermission: (spaceKey, data) => api.post(`/spaces/${spaceKey}/permissions`, data),
  revokePermission: (spaceKey, subjectType, subjectId) =>
    api.delete(`/spaces/${spaceKey}/permissions`, { params: { subjectType, subjectId } })
}
```
  </action>
  <verify>cd frontend && npm run build 2>&1 | tail -10</verify>
  <acceptance_criteria>
    - frontend/src/api/permission.js에 getSpacePermissions, grantPermission, revokePermission 존재
    - 빌드 성공
  </acceptance_criteria>
  <done>permission API 클라이언트 완성</done>
</task>

<task type="auto">
  <name>Task 2: VersionHistoryPanel 슬라이드 패널</name>
  <files>
    frontend/src/components/content/VersionHistoryPanel.vue
  </files>
  <read_first>
    frontend/src/stores/content.js,
    frontend/src/api/content.js
  </read_first>
  <action>
VersionHistoryPanel.vue:
- props: visible(Boolean), contentId(Number)
- emits: ['update:visible']
- DxDrawer 또는 DxPopup(position="right", width=400)으로 슬라이드 패널 구현
- 패널 닫기: emit('update:visible', false)
- 버전 목록: contentApi.getVersions(contentId) 호출
  DxList 또는 v-for로 버전 목록 표시
  각 항목: versionNo, 작성자, 날짜, "이 버전 보기" 버튼
- watch(visible): visible=true 시 버전 목록 새로고침
- 스타일: 패널 헤더 "버전 기록", 닫기 버튼(X)

ContentView.vue 업데이트 (기존 파일에 추가):
- showVersionPanel: ref(false)
- VersionHistoryPanel 컴포넌트 임포트 및 추가:
  ```html
  <VersionHistoryPanel v-model:visible="showVersionPanel" :contentId="currentContent?.id" />
  ```
- "버전 기록" 버튼에 @click="showVersionPanel = true" 연결
  </action>
  <verify>cd frontend && npm run build 2>&1 | tail -10</verify>
  <acceptance_criteria>
    - VersionHistoryPanel.vue에 DxDrawer 또는 DxPopup 사용
    - props에 visible, contentId 정의
    - ContentView.vue에 VersionHistoryPanel 임포트 및 v-model:visible 바인딩 존재
    - 빌드 성공
  </acceptance_criteria>
  <done>VersionHistoryPanel 완성 + ContentView 업데이트, 빌드 성공</done>
</task>

<task type="auto">
  <name>Task 3: SpacePermissionView — 권한 관리 화면</name>
  <files>
    frontend/src/views/space/SpacePermissionView.vue,
    frontend/src/router/index.js
  </files>
  <read_first>
    frontend/src/api/permission.js,
    frontend/src/views/space/SpaceListView.vue,
    frontend/src/router/index.js
  </read_first>
  <action>
SpacePermissionView.vue:
- route.params.spaceKey 로 Space 특정
- AppHeader + 메인 영역
- 메인 영역 제목: "{spaceName} — 권한 관리"
- 권한 목록 (DxDataGrid):
  컬럼: 대상 유형(USER/GROUP/ALL), 대상 ID, 권한 레벨(SPACE_ADMIN/WRITE/READ/NONE), 생성일
  - 삭제 버튼(행별)
- 권한 추가 다이얼로그 (DxPopup):
  - DxForm:
    - subjectType: DxSelectBox ["USER", "GROUP", "ALL"]
    - subjectId: DxNumberBox (ALL이면 비활성화)
    - permissionLevel: DxSelectBox ["SPACE_ADMIN", "WRITE", "READ", "NONE"]
  - 저장 버튼 → permissionApi.grantPermission(spaceKey, formData)
- "권한 추가" 버튼 → 다이얼로그 열기
- onMounted: permissionApi.getSpacePermissions(spaceKey)

router/index.js에 라우트 추가:
{ path: '/spaces/:spaceKey/permissions', component: SpacePermissionView }
또한 SpaceHomeView에 "권한 설정" 링크 추가 → `/spaces/${spaceKey}/permissions`
  </action>
  <verify>cd frontend && npm run build 2>&1 | tail -10</verify>
  <acceptance_criteria>
    - SpacePermissionView.vue에 DxDataGrid 권한 목록 존재
    - DxPopup 권한 추가 다이얼로그 존재
    - router에 /spaces/:spaceKey/permissions 라우트 존재
    - 빌드 성공
  </acceptance_criteria>
  <done>SpacePermissionView + 라우터 업데이트 완성, 빌드 성공</done>
</task>

<task type="auto">
  <name>Task 4: Phase 1 최종 빌드 검증 및 커밋</name>
  <files></files>
  <read_first>
    backend/src/main/java/com/company/wiki/WikiApplication.java
  </read_first>
  <action>
1. 프론트엔드 최종 빌드 확인:
   cd /Users/shinwon/confluence-clone-proj/frontend && npm run build 2>&1 | tail -5

2. 백엔드 전체 테스트 실행:
   cd /Users/shinwon/confluence-clone-proj/backend && export PATH="/opt/homebrew/opt/openjdk@21/bin:$PATH" && ./mvnw test -Dspring.profiles.active=local 2>&1 | tail -20

3. Phase 1 완료 태그:
   git -C /Users/shinwon/confluence-clone-proj add -A
   git -C /Users/shinwon/confluence-clone-proj commit -m "feat: Phase 1 complete — User/Group/Space/Content/Permission core features"
   git -C /Users/shinwon/confluence-clone-proj tag -a v0.2.0-phase1 -m "Phase 1 complete: core CRUD features"
  </action>
  <verify>cd /Users/shinwon/confluence-clone-proj/frontend && npm run build 2>&1 | grep -E "built|error" && cd /Users/shinwon/confluence-clone-proj/backend && export PATH="/opt/homebrew/opt/openjdk@21/bin:$PATH" && ./mvnw test -Dspring.profiles.active=local 2>&1 | grep -E "Tests run|BUILD"</verify>
  <acceptance_criteria>
    - 프론트엔드 빌드 성공
    - 백엔드 전체 테스트 BUILD SUCCESS
    - 커밋 + 태그 생성
  </acceptance_criteria>
  <done>Phase 1 완료 검증 + 태그 v0.2.0-phase1 생성</done>
</task>

</tasks>

<verification>
- [ ] cd frontend && npm run build — 에러 없음
- [ ] cd backend && ./mvnw test — BUILD SUCCESS
- [ ] git tag -l | grep phase1 — v0.2.0-phase1 존재
- [ ] VersionHistoryPanel이 ContentView에서 v-model:visible로 제어됨 확인
</verification>

<success_criteria>
- 프론트엔드 빌드 성공
- 백엔드 전체 테스트 통과
- VersionHistoryPanel, SpacePermissionView 완성
- v0.2.0-phase1 태그 생성
</success_criteria>

<output>
After completion, create `.planning/phases/01-core/01-07-SUMMARY.md`
</output>
