---
phase: 04-admin
plan: 06
type: execute
wave: 3
depends_on:
  - 04-04
files_modified:
  - frontend/src/api/notification.js
  - frontend/src/stores/notification.js
  - frontend/src/components/AppHeader.vue
  - frontend/src/App.vue
autonomous: false
requirements:
  - NOTIF-02

must_haves:
  truths:
    - "AppHeader에 알림 벨 아이콘이 표시되고 미읽음 수 배지가 숫자로 표시된다"
    - "벨 아이콘 클릭 시 최근 알림 목록 드롭다운이 열린다"
    - "드롭다운에서 개별 알림 클릭 시 읽음 처리되고 배지 카운트가 감소한다"
    - "'모두 읽음' 버튼 클릭 시 모든 알림이 읽음 처리되고 배지가 0이 된다"
    - "App.vue onMounted에서 폴링이 시작되어 30초마다 미읽음 카운트가 갱신된다"
  artifacts:
    - path: "frontend/src/api/notification.js"
      provides: "알림 API 모듈 (named export)"
      contains: "notificationApi"
    - path: "frontend/src/stores/notification.js"
      provides: "Pinia 알림 스토어 (unreadCount, notifications[], polling)"
      contains: "useNotificationStore"
    - path: "frontend/src/components/AppHeader.vue"
      provides: "알림 벨 아이콘 + 드롭다운 패널 통합"
      contains: "unreadCount"
  key_links:
    - from: "App.vue onMounted()"
      to: "notificationStore.startPolling()"
      via: "단일 진입점 폴링 시작 — 중복 등록 방지"
    - from: "AppHeader.vue 벨 아이콘 클릭"
      to: "notificationStore.fetchNotifications()"
      via: "드롭다운 열기 시 최신 목록 로드"
    - from: "notificationStore.unreadCount"
      to: "AppHeader.vue 배지 표시"
      via: "반응형 ref — storeToRefs(notificationStore)"
---

<objective>
알림 프론트엔드를 구현한다: notification.js API 모듈, Pinia 알림 스토어(폴링 포함), AppHeader 알림 벨 아이콘 + 드롭다운 패널.

Purpose: 사용자가 댓글 알림을 실시간(30초 폴링)으로 확인하고 AppHeader에서 읽음 처리할 수 있게 한다.

Output: frontend/src/api/notification.js, frontend/src/stores/notification.js, AppHeader.vue 업데이트, App.vue 폴링 시작 추가
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-admin/04-CONTEXT.md
@.planning/phases/04-admin/04-RESEARCH.md
@.planning/phases/04-admin/04-04-SUMMARY.md

<interfaces>
<!-- NotificationController 엔드포인트 (04-04에서 구현) -->
<!--
  GET /notifications?isRead=false&page=0&size=20 → PagedResponse<NotificationDto.Response>
  GET /notifications/unread-count → Long
  PATCH /notifications/{id}/read → void (200)
  PATCH /notifications/read-all → void (200)
-->

<!-- NotificationDto.Response 필드 -->
<!--
  { id, userId, type, title, message, isRead, linkUrl, createdAt }
-->

<!-- Pinia store 패턴 (auth.js) -->
<!--
  export const useAuthStore = defineStore('auth', () => {
    const user = ref(null)
    return { user, ... }
  })
-->

<!-- DevExtreme Popover (알림 드롭다운 대안) — 커스텀 드롭다운 구현 권장 -->
<!-- AppHeader.vue 현재 상태 — 존재 여부 확인 필요 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: notification.js API 모듈 + Pinia 알림 스토어 생성</name>
  <files>
    frontend/src/api/notification.js,
    frontend/src/stores/notification.js
  </files>
  <read_first>
    - frontend/src/api/space.js (API 모듈 패턴 참조 — default export)
    - frontend/src/api/mail.js (named export 패턴 확인)
    - frontend/src/stores/auth.js (Pinia defineStore Composition API 패턴 확인)
    - frontend/src/api/axios.js (axios 인스턴스 확인 — JWT 인터셉터 포함 여부)
  </read_first>
  <action>
    1. frontend/src/api/notification.js를 named export 방식으로 생성한다:
       import api from './axios'
       export const notificationApi = {
         getNotifications: (params) => api.get('/notifications', { params }),
         getUnreadCount: () => api.get('/notifications/unread-count'),
         markRead: (id) => api.patch(`/notifications/${id}/read`),
         markAllRead: () => api.patch('/notifications/read-all')
       }

    2. frontend/src/stores/notification.js를 Composition API defineStore 방식으로 생성한다:
       export const useNotificationStore = defineStore('notification', () => {
         const unreadCount = ref(0)
         const notifications = ref([])
         let pollTimer = null

         async function fetchUnreadCount() {
           try { const { data } = await notificationApi.getUnreadCount(); unreadCount.value = data.data } catch (e) { /* 폴링 실패 무시 */ }
         }

         async function fetchNotifications() {
           const { data } = await notificationApi.getNotifications({ page: 0, size: 20 })
           notifications.value = data.data.content
         }

         async function markRead(id) {
           await notificationApi.markRead(id)
           notifications.value = notifications.value.map(n => n.id === id ? { ...n, isRead: true } : n)
           if (unreadCount.value > 0) unreadCount.value--
         }

         async function markAllRead() {
           await notificationApi.markAllRead()
           unreadCount.value = 0
           notifications.value = notifications.value.map(n => ({ ...n, isRead: true }))
         }

         function startPolling() {
           fetchUnreadCount()
           if (pollTimer) clearInterval(pollTimer)  // 중복 등록 방지
           pollTimer = setInterval(fetchUnreadCount, 30000)
         }

         function stopPolling() { if (pollTimer) { clearInterval(pollTimer); pollTimer = null } }

         return { unreadCount, notifications, fetchUnreadCount, fetchNotifications, markRead, markAllRead, startPolling, stopPolling }
       })
  </action>
  <verify>
    <automated>cd /Users/shinwon/confluence-clone-proj/frontend && node -e "const f=require('fs'); const c=f.readFileSync('src/api/notification.js','utf8'); if(!c.includes('notificationApi')) throw new Error('fail'); if(!c.includes('markAllRead')) throw new Error('fail'); console.log('notification.js OK');" && node -e "const f=require('fs'); const c=f.readFileSync('src/stores/notification.js','utf8'); if(!c.includes('useNotificationStore')) throw new Error('fail'); if(!c.includes('startPolling')) throw new Error('fail'); console.log('store OK');"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/api/notification.js에 export const notificationApi 와 markAllRead 메서드가 존재한다
    - frontend/src/stores/notification.js에 useNotificationStore, startPolling, stopPolling이 존재한다
    - stores/notification.js의 startPolling()에 pollTimer가 이미 존재하면 clearInterval하는 중복 방지 코드가 있다
    - fetchUnreadCount가 try-catch로 감싸져 폴링 실패 시 오류가 발생하지 않는다
  </acceptance_criteria>
  <done>notification.js API 모듈 + Pinia 알림 스토어 생성 완료</done>
</task>

<task type="auto">
  <name>Task 2: AppHeader.vue 알림 벨 아이콘 + 드롭다운 추가 + App.vue 폴링 시작</name>
  <files>
    frontend/src/components/AppHeader.vue,
    frontend/src/App.vue
  </files>
  <read_first>
    - frontend/src/components/AppHeader.vue (현재 구조 확인 — 벨 아이콘 추가 위치, 기존 import/setup 구조)
    - frontend/src/App.vue (onMounted 존재 여부 확인 — startPolling 추가 위치)
    - frontend/src/stores/auth.js (isLoggedIn 확인 — 로그인 상태에서만 폴링 시작)
    - frontend/src/stores/notification.js (Task 1에서 생성한 스토어 — storeToRefs 사용 확인)
  </read_first>
  <action>
    1. AppHeader.vue에 알림 벨 아이콘과 드롭다운을 추가한다.
       - import: { useNotificationStore } from '@/stores/notification', { storeToRefs } from 'pinia'
       - setup: const notificationStore = useNotificationStore(); const { unreadCount, notifications } = storeToRefs(notificationStore); const showDropdown = ref(false)
       - 벨 아이콘 버튼: &lt;button @click="toggleDropdown" class="notif-bell"&gt;🔔 &lt;span v-if="unreadCount > 0" class="badge"&gt;{{ unreadCount }}&lt;/span&gt;&lt;/button&gt;
       - 드롭다운 패널 (v-if="showDropdown"):
         - 알림 목록 (v-for="n in notifications"):
           각 항목 클릭 시 notificationStore.markRead(n.id) + router.push(n.linkUrl)
           읽지 않은 항목은 배경 강조 (:class="{ unread: !n.isRead }")
         - '모두 읽음' 버튼: @click="notificationStore.markAllRead()"
         - 드롭다운 열기 시 notificationStore.fetchNotifications() 호출 (최신 목록 로드)
       - toggleDropdown(): showDropdown.value = !showDropdown.value; if(showDropdown.value) notificationStore.fetchNotifications()
       - 드롭다운 외부 클릭 닫기: v-click-outside 또는 @blur 패턴 (DevExtreme 없이 구현, 단순하게 showDropdown.value=false on document click)

    2. App.vue의 onMounted에서 startPolling 단일 호출을 추가한다:
       - import: { useNotificationStore } from '@/stores/notification', { useAuthStore } from '@/stores/auth'
       - onMounted: const auth = useAuthStore(); const notif = useNotificationStore(); if (auth.isLoggedIn) notif.startPolling()
       - onUnmounted 또는 watch(auth.isLoggedIn): 로그아웃 시 notif.stopPolling()
       - 로그인 watch: watch(() => auth.isLoggedIn, (v) => { if(v) notif.startPolling(); else notif.stopPolling() })
       - AppHeader가 이미 App.vue에 포함되어 있다면 중복 startPolling 호출이 없는지 확인한다.
  </action>
  <verify>
    <automated>cd /Users/shinwon/confluence-clone-proj/frontend && npm run build -- --mode development 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - AppHeader.vue에 useNotificationStore import 구문이 존재한다
    - AppHeader.vue에 unreadCount 참조 코드와 v-if="unreadCount > 0" 조건부 배지가 존재한다
    - AppHeader.vue에 notificationStore.markAllRead() 호출이 존재한다
    - App.vue에 notif.startPolling() 호출이 존재하고 auth.isLoggedIn 조건 체크가 포함된다
    - npm run build가 에러 없이 완료된다
  </acceptance_criteria>
  <done>AppHeader 알림 벨 아이콘 + 드롭다운 구현 완료, App.vue 폴링 시작 추가, 빌드 성공</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    알림 프론트엔드 — AppHeader 알림 벨 아이콘 + 드롭다운 + Pinia 스토어 폴링.
    30초 간격 미읽음 카운트 자동 갱신.
  </what-built>
  <how-to-verify>
    1. 백엔드 실행: JAVA_HOME=/opt/homebrew/opt/openjdk@21 ./mvnw spring-boot:run -pl backend
    2. 프론트엔드 실행: cd frontend && npm run dev
    3. http://localhost:3000 에서 admin / Admin1234! 로그인
    4. 확인 항목:
       a. AppHeader에 🔔 벨 아이콘이 표시되는가?
       b. 벨 아이콘 클릭 시 드롭다운 패널이 열리는가?
       c. 브라우저 네트워크 탭에서 /api/notifications/unread-count 요청이 30초 간격으로 1번씩만 발생하는가? (중복 폴링 없음)
       d. 알림이 있는 경우 배지에 숫자가 표시되는가?
       e. 드롭다운에서 '모두 읽음' 클릭 시 배지가 0으로 변경되는가?
  </how-to-verify>
  <resume-signal>UI가 올바르게 동작하면 "approved" 입력. 문제 발생 시 오류 내용을 설명해주세요.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AppHeader.vue → notificationApi | JWT Bearer 자동 주입 (axios 인터셉터) |
| 알림 드롭다운 linkUrl | 서버에서 반환된 URL — 내부 경로만 사용, 외부 URL 리다이렉트 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | Denial of Service | 알림 폴링 중복 등록 | mitigate | stores/notification.js startPolling()에서 기존 pollTimer clearInterval 후 재등록; App.vue 단일 지점에서만 startPolling() 호출 |
| T-04-06-02 | Information Disclosure | 알림 linkUrl XSS | accept | router.push(n.linkUrl) — Vue Router 내부 경로만 처리; 외부 도메인은 Vue Router가 차단 |
| T-04-06-03 | Tampering | 알림 읽음 처리 IDOR | mitigate | 백엔드 NotificationService.markAsRead()에서 userId 검증 (04-02 구현) — 프론트엔드는 본인 알림만 표시하므로 추가 방어 |
| T-04-06-SC | Tampering | npm/pip/cargo installs | accept | 이 플랜은 신규 패키지 설치 없음 |
</threat_model>

<verification>
1. 프론트엔드 빌드 성공: `npm run build` 에러 없음
2. AppHeader 알림 벨 아이콘 + 드롭다운 동작 확인 (checkpoint)
3. 폴링 중복 없음 확인 — 네트워크 탭에서 30초마다 1회 요청 (checkpoint)
4. Wave 3 전체 완료 후 전체 백엔드 테스트: `JAVA_HOME=/opt/homebrew/opt/openjdk@21 ./mvnw test -pl backend`
</verification>

<success_criteria>
- notification.js API 모듈 + Pinia 알림 스토어 생성 완료
- AppHeader 알림 벨 아이콘 + 미읽음 배지 + 드롭다운 패널 구현 완료
- App.vue 단일 onMounted에서 폴링 시작 (중복 없음)
- 빌드 성공 + human-verify checkpoint 통과
- Phase 4 전체 요구사항 ADMIN-01, ADMIN-02, NOTIF-01, NOTIF-02, AUDIT-01, AUDIT-02 모두 충족
</success_criteria>

<output>
Create `.planning/phases/04-admin/04-06-SUMMARY.md` when done
</output>
