---
phase: 02-mail
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/company/wiki/common/util/AesEncryptUtil.java
  - backend/src/main/java/com/company/wiki/mail/entity/MailAccount.java
  - backend/src/main/java/com/company/wiki/mail/repository/MailAccountRepository.java
  - backend/src/main/java/com/company/wiki/mail/dto/MailAccountDto.java
  - backend/src/main/java/com/company/wiki/mail/service/MailAccountService.java
  - backend/src/main/java/com/company/wiki/mail/controller/MailAccountController.java
  - backend/src/main/resources/application.yml
  - backend/src/test/java/com/company/wiki/mail/controller/MailAccountControllerTest.java
autonomous: true
requirements: [REQ-MAIL-ACCOUNT]
must_haves:
  truths:
    - "POST /spaces/{key}/mail-accounts 로 메일 계정을 등록할 수 있다"
    - "GET /spaces/{key}/mail-accounts 로 메일 계정 목록을 조회할 수 있다"
    - "DELETE /spaces/{key}/mail-accounts/{id} 로 메일 계정을 삭제할 수 있다"
    - "credential 필드는 AES-256으로 암호화되어 DB에 저장된다"
  artifacts:
    - path: backend/src/main/java/com/company/wiki/mail/entity/MailAccount.java
      provides: MailAccount 엔티티
      min_lines: 40
    - path: backend/src/main/java/com/company/wiki/common/util/AesEncryptUtil.java
      provides: AES-256 암복호화 유틸
      min_lines: 30
---

<objective>
메일 계정 CRUD API와 AES-256 자격증명 암호화를 구현한다.

Purpose: IMAP 메일 동기화의 기반 — 메일 계정 등록/관리가 없으면 폴링 불가.
Output: AesEncryptUtil, MailAccount 엔티티, MailAccountService, MailAccountController + 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@backend/src/main/resources/db/migration/V5__mail.sql
@backend/src/main/java/com/company/wiki/common/exception/ErrorCode.java
@backend/src/main/java/com/company/wiki/common/response/ApiResponse.java
@backend/src/main/java/com/company/wiki/space/repository/SpaceRepository.java
@backend/src/main/java/com/company/wiki/permission/service/PermissionService.java
@backend/src/main/resources/application.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: AesEncryptUtil (AES-256/CBC/PKCS5Padding)</name>
  <files>
    backend/src/main/java/com/company/wiki/common/util/AesEncryptUtil.java,
    backend/src/main/resources/application.yml
  </files>
  <read_first>
    backend/src/main/java/com/company/wiki/common/util/JwtProvider.java,
    backend/src/main/resources/application.yml
  </read_first>
  <action>
AesEncryptUtil.java (@Component):
- @Value("${aes.secret-key}") String secretKey (32바이트)
- @Value("${aes.iv}") String iv (16바이트)
- String encrypt(String plaintext): AES/CBC/PKCS5Padding, Base64 인코딩 반환
- String decrypt(String ciphertext): Base64 디코딩 후 복호화
- SecretKeySpec(secretKey.getBytes(UTF_8), "AES"), IvParameterSpec(iv.getBytes(UTF_8))

application.yml에 추가:
aes:
  secret-key: ${AES_SECRET_KEY:WikiAesKey256BitSecretKey12345678}
  iv: ${AES_IV:WikiAesIV16Bytes!}
  </action>
  <verify>cd backend && export JAVA_HOME="/opt/homebrew/opt/openjdk@21" && export PATH="$JAVA_HOME/bin:$PATH" && ./mvnw compile -q 2>&1 | tail -5</verify>
  <acceptance_criteria>
    - AesEncryptUtil.java에 encrypt, decrypt 메서드 존재
    - application.yml에 aes.secret-key 설정 존재
    - 컴파일 성공
  </acceptance_criteria>
  <done>AesEncryptUtil 컴파일 성공</done>
</task>

<task type="auto">
  <name>Task 2: MailAccount 엔티티 + 레포지토리</name>
  <files>
    backend/src/main/java/com/company/wiki/mail/entity/MailAccount.java,
    backend/src/main/java/com/company/wiki/mail/repository/MailAccountRepository.java
  </files>
  <read_first>backend/src/main/resources/db/migration/V5__mail.sql</read_first>
  <action>
MailAccount.java (@Entity @Table(name="mail_accounts") @Getter @Setter @NoArgsConstructor @Builder @AllArgsConstructor):
- id(Long IDENTITY), spaceId(Long NOT NULL @Column), emailAddress(VARCHAR 300), imapHost(VARCHAR 500)
- imapPort(int 기본 993), imapSsl(boolean 기본 true), smtpHost(VARCHAR 500), smtpPort(int 기본 587)
- credential(TEXT NOT NULL), syncStatus(VARCHAR 20 기본 "PENDING"), lastSyncedAt(LocalDateTime nullable), createdAt(LocalDateTime)
- @PrePersist: createdAt=now(), syncStatus/imapPort/smtpPort 기본값

MailAccountRepository: JpaRepository<MailAccount, Long>
- List<MailAccount> findBySpaceId(Long spaceId)
- List<MailAccount> findBySyncStatus(String syncStatus)
- Optional<MailAccount> findByIdAndSpaceId(Long id, Long spaceId)
  </action>
  <verify>cd backend && export JAVA_HOME="/opt/homebrew/opt/openjdk@21" && export PATH="$JAVA_HOME/bin:$PATH" && ./mvnw compile -q 2>&1 | tail -5</verify>
  <acceptance_criteria>
    - MailAccount.java에 credential, syncStatus, imapHost 필드 존재
    - 컴파일 성공
  </acceptance_criteria>
  <done>MailAccount 엔티티 컴파일 성공</done>
</task>

<task type="auto">
  <name>Task 3: MailAccountDto + MailAccountService</name>
  <files>
    backend/src/main/java/com/company/wiki/mail/dto/MailAccountDto.java,
    backend/src/main/java/com/company/wiki/mail/service/MailAccountService.java
  </files>
  <read_first>
    backend/src/main/java/com/company/wiki/mail/entity/MailAccount.java,
    backend/src/main/java/com/company/wiki/common/util/AesEncryptUtil.java,
    backend/src/main/java/com/company/wiki/space/repository/SpaceRepository.java
  </read_first>
  <action>
MailAccountDto.java:
- CreateRequest: emailAddress(@NotBlank), password(@NotBlank), imapHost(@NotBlank), imapPort(int 기본 993), imapSsl(boolean 기본 true), smtpHost, smtpPort(int 기본 587)
- Response: id, spaceId, emailAddress, imapHost, imapPort, imapSsl, syncStatus, lastSyncedAt, createdAt
- static Response from(MailAccount account)

MailAccountService.java (@Service @Transactional @RequiredArgsConstructor):
- SpaceRepository, MailAccountRepository, AesEncryptUtil, PermissionService 주입
- List<MailAccountDto.Response> findBySpace(String spaceKey):
  → spaceRepo.findBySpaceKeyAndDeletedAtIsNull(spaceKey).orElseThrow(SPACE_NOT_FOUND)
  → findBySpaceId(space.getId())
- MailAccountDto.Response create(String spaceKey, MailAccountDto.CreateRequest req, Long userId, String userRole, List<Long> groupIds):
  → PermissionService.isSpaceAdmin(...) 체크 → 아니면 throw FORBIDDEN
  → credential = aesEncryptUtil.encrypt(req.getPassword())
  → MailAccount 빌드 후 저장
- void delete(String spaceKey, Long accountId, Long userId, String userRole, List<Long> groupIds):
  → isSpaceAdmin 체크
  → findByIdAndSpaceId → MAIL_ACCOUNT_NOT_FOUND
  → deleteById
- void updateSyncStatus(Long accountId, String status):
  → findById → status 업데이트 → save
  </action>
  <verify>cd backend && export JAVA_HOME="/opt/homebrew/opt/openjdk@21" && export PATH="$JAVA_HOME/bin:$PATH" && ./mvnw compile -q 2>&1 | tail -5</verify>
  <acceptance_criteria>
    - MailAccountService에 create, delete, updateSyncStatus 존재
    - create에서 aesEncryptUtil.encrypt 사용
    - 컴파일 성공
  </acceptance_criteria>
  <done>MailAccountService 완성, 컴파일 성공</done>
</task>

<task type="auto">
  <name>Task 4: MailAccountController + 통합 테스트</name>
  <files>
    backend/src/main/java/com/company/wiki/mail/controller/MailAccountController.java,
    backend/src/test/java/com/company/wiki/mail/controller/MailAccountControllerTest.java
  </files>
  <read_first>
    backend/src/main/java/com/company/wiki/mail/service/MailAccountService.java,
    backend/src/test/java/com/company/wiki/space/controller/SpaceControllerTest.java
  </read_first>
  <action>
MailAccountController.java (@RestController @RequestMapping("/spaces/{spaceKey}/mail-accounts") @RequiredArgsConstructor):
- getUserId(UserDetails p): Long.parseLong(p.getUsername())
- getRole(UserDetails p): p.getAuthorities().iterator().next().getAuthority().replace("ROLE_","")
- getGroupIds(): List.of() (단순화 — 그룹 권한은 추후 통합)
- GET → findBySpace
- POST → create(@Valid req, currentUser)
- DELETE /{id} → delete, 204

MailAccountControllerTest.java (@SpringBootTest @AutoConfigureMockMvc @ActiveProfiles("local") @Transactional):
- 헬퍼: adminToken() — POST /auth/login { admin/Admin1234! }
- 헬퍼: createSpace(token) — POST /spaces { spaceKey:"MAIL", name:"메일 테스트" }
- Test 1: createMailAccount_success() — POST /spaces/MAIL/mail-accounts { emailAddress:"test@test.com", password:"pass", imapHost:"imap.test.com" } → 200
- Test 2: getMailAccounts_returnsList() — 생성 후 GET → 200, size >= 1
- Test 3: deleteMailAccount_success() — 생성 → DELETE /{id} → 204
- Test 4: credential_isEncrypted() — 생성 후 MailAccountRepository로 직접 조회 → credential != "pass"

테스트 실행: cd backend && export JAVA_HOME="/opt/homebrew/opt/openjdk@21" && export PATH="$JAVA_HOME/bin:$PATH" && ./mvnw test -Dtest=MailAccountControllerTest -Dspring.profiles.active=local 2>&1 | tail -10
  </action>
  <verify>cd backend && export JAVA_HOME="/opt/homebrew/opt/openjdk@21" && export PATH="$JAVA_HOME/bin:$PATH" && ./mvnw test -Dtest=MailAccountControllerTest -Dspring.profiles.active=local 2>&1 | tail -10</verify>
  <acceptance_criteria>
    - Tests run: 4 이상, BUILD SUCCESS, Failures: 0
  </acceptance_criteria>
  <done>모든 테스트 통과, BUILD SUCCESS</done>
</task>

</tasks>

<verification>
- [ ] ./mvnw test -Dtest=MailAccountControllerTest -Dspring.profiles.active=local — BUILD SUCCESS
- [ ] credential != plaintext password 확인
</verification>

<success_criteria>
- MailAccountControllerTest 모든 테스트 통과
- AES-256 암호화 동작 확인
</success_criteria>

<output>
After completion, create `.planning/phases/02-mail/02-01-SUMMARY.md`
</output>
