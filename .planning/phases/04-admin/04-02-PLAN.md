---
phase: 04-admin
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/company/wiki/notification/entity/Notification.java
  - backend/src/main/java/com/company/wiki/notification/repository/NotificationRepository.java
  - backend/src/main/java/com/company/wiki/notification/dto/NotificationDto.java
  - backend/src/main/java/com/company/wiki/notification/service/NotificationService.java
  - backend/src/main/java/com/company/wiki/comment/service/CommentService.java
  - backend/src/test/java/com/company/wiki/notification/service/NotificationServiceTest.java
autonomous: true
requirements:
  - NOTIF-01

must_haves:
  truths:
    - "댓글 생성 후 notifications 테이블에 COMMENT 타입 레코드가 콘텐츠 작성자 user_id로 저장된다"
    - "댓글 작성자가 콘텐츠 작성자 본인인 경우 알림이 생성되지 않는다"
    - "NotificationService.create() 실패가 CommentService.createComment() 트랜잭션을 롤백시키지 않는다"
    - "NotificationService.markAsRead()는 다른 사용자의 알림을 수정하지 못한다 (userId 불일치 시 BusinessException)"
  artifacts:
    - path: "backend/src/main/java/com/company/wiki/notification/entity/Notification.java"
      provides: "notifications 테이블 JPA 엔티티"
      contains: "class Notification"
    - path: "backend/src/main/java/com/company/wiki/notification/service/NotificationService.java"
      provides: "알림 생성·조회·읽음처리 서비스"
      exports: ["create", "getNotifications", "getUnreadCount", "markAsRead", "markAllAsRead"]
    - path: "backend/src/test/java/com/company/wiki/notification/service/NotificationServiceTest.java"
      provides: "NOTIF-01 통합 테스트"
      contains: "createComment_createsNotification"
  key_links:
    - from: "CommentService.createComment()"
      to: "NotificationService.create(contentAuthorId, 'COMMENT', ...)"
      via: "직접 호출 (try-catch 래핑)"
    - from: "NotificationService.markAsRead(id, userId)"
      to: "NotificationRepository.findByIdAndUserId(id, userId)"
      via: "IDOR 방지 — userId 검증 필수"
---

<objective>
알림 도메인의 전체 백엔드를 구현한다: Notification 엔티티/리포지토리/서비스를 신규 생성하고, CommentService에 NotificationService.create() 직접 호출을 통합한다.

Purpose: 댓글 생성·멘션 이벤트 시 notifications 테이블에 적재한다. Wave 2의 NotificationController(04-04)가 이 서비스를 사용하므로 먼저 완료되어야 한다.

Output: Notification 엔티티, NotificationRepository, NotificationDto, NotificationService, CommentService 통합, 통합 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-admin/04-CONTEXT.md
@.planning/phases/04-admin/04-RESEARCH.md

<interfaces>
<!-- V7 notifications 스키마 (이미 마이그레이션 완료) -->
<!--
  notifications(
    id BIGSERIAL PK,
    user_id BIGINT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    type VARCHAR(50) NOT NULL,
    title VARCHAR(500) NOT NULL,
    message TEXT,
    is_read BOOLEAN NOT NULL DEFAULT FALSE,
    link_url VARCHAR(1000),
    created_at TIMESTAMP NOT NULL DEFAULT NOW()
  )
-->

<!-- CommentService.createComment() 현재 시그니처 -->
<!--
  public CommentDto.CommentNode createComment(Long contentId, CommentDto.CreateRequest req, Long userId, String role)
  현재: Comment 저장 후 buildCommentNode() 반환 — 이 직후에 알림 생성 try-catch 블록 추가
  content.getCreatedBy()가 User 객체인지 Long userId인지 실제 파일 확인 필요
-->

<!-- ErrorCode — NOTIFICATION_NOT_FOUND는 04-01에서 추가됨 (depends_on은 없으나 Wave 1 병렬 실행 순서 주의) -->
<!-- 이 플랜 자체에서도 ErrorCode.NOTIFICATION_NOT_FOUND를 추가하는 방어 코드 포함 필요 -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Notification 엔티티·리포지토리·DTO·서비스 생성</name>
  <files>
    backend/src/main/java/com/company/wiki/notification/entity/Notification.java,
    backend/src/main/java/com/company/wiki/notification/repository/NotificationRepository.java,
    backend/src/main/java/com/company/wiki/notification/dto/NotificationDto.java,
    backend/src/main/java/com/company/wiki/notification/service/NotificationService.java,
    backend/src/test/java/com/company/wiki/notification/service/NotificationServiceTest.java
  </files>
  <read_first>
    - backend/src/main/resources/db/migration/V7__audit_notifications.sql (notifications 스키마 컬럼명 확인)
    - backend/src/main/java/com/company/wiki/common/exception/ErrorCode.java (NOTIFICATION_NOT_FOUND 존재 여부 확인 — 없으면 추가)
    - backend/src/main/java/com/company/wiki/comment/service/CommentService.java (서비스 패턴 참조)
    - backend/src/main/java/com/company/wiki/common/response/ApiResponse.java
  </read_first>
  <behavior>
    - Test 1: NotificationService.create(userId, "COMMENT", "새 댓글", "내용에 댓글", "/link") 호출 후 notificationRepository.countByUserIdAndIsReadFalse(userId) == 1
    - Test 2: NotificationService.getUnreadCount(userId)는 is_read=false 레코드만 카운트한다
    - Test 3: NotificationService.markAsRead(id, 다른userId)는 BusinessException(NOTIFICATION_NOT_FOUND)을 던진다 (IDOR 방지)
    - Test 4: NotificationService.markAllAsRead(userId) 호출 후 해당 userId의 모든 알림 isRead=true
  </behavior>
  <action>
    1. Notification 엔티티를 com.company.wiki.notification.entity 패키지에 생성한다. 필드: id (BIGSERIAL), userId (user_id BIGINT), type (VARCHAR 50), title (VARCHAR 500), message (TEXT), isRead (is_read BOOLEAN), linkUrl (link_url VARCHAR 1000), createdAt (created_at TIMESTAMP). @Entity, @Table(name="notifications"), @Getter, @Setter, @Builder, @NoArgsConstructor, @AllArgsConstructor 적용. @PrePersist로 createdAt = LocalDateTime.now() 설정.

    2. NotificationRepository를 com.company.wiki.notification.repository 패키지에 생성한다. JpaRepository&lt;Notification, Long&gt; 확장. 추가 메서드:
       - Page&lt;Notification&gt; findByUserIdAndIsRead(Long userId, Boolean isRead, Pageable pageable)
       - Page&lt;Notification&gt; findByUserId(Long userId, Pageable pageable)
       - long countByUserIdAndIsReadFalse(Long userId)
       - Optional&lt;Notification&gt; findByIdAndUserId(Long id, Long userId)
       - @Modifying @Query("UPDATE Notification n SET n.isRead = true WHERE n.userId = :userId AND n.isRead = false") void markAllAsReadByUserId(@Param("userId") Long userId)

    3. NotificationDto를 com.company.wiki.notification.dto 패키지에 생성한다. 내부 record Response(Long id, Long userId, String type, String title, String message, boolean isRead, String linkUrl, LocalDateTime createdAt). 정적 팩터리 메서드 from(Notification n).

    4. NotificationService를 com.company.wiki.notification.service 패키지에 생성한다. @Service, @RequiredArgsConstructor.
       - create(Long userId, String type, String title, String message, String linkUrl): Notification 저장. 예외는 호출자가 try-catch로 처리하므로 여기서는 예외 전파.
       - getNotifications(Long userId, Boolean isRead, Pageable pageable): isRead가 null이면 findByUserId, 아니면 findByUserIdAndIsRead 호출. PagedResponse&lt;NotificationDto.Response&gt; 반환. PagedResponse record를 동일 파일 또는 별도 common 클래스에 정의.
       - getUnreadCount(Long userId): countByUserIdAndIsReadFalse(userId) 반환.
       - markAsRead(Long notificationId, Long userId): findByIdAndUserId로 조회 (없으면 BusinessException(ErrorCode.NOTIFICATION_NOT_FOUND)), setRead(true) 후 save.
       - markAllAsRead(Long userId): markAllAsReadByUserId(userId) 호출.

    5. ErrorCode.java에 NOTIFICATION_NOT_FOUND가 없으면 추가: NOTIFICATION_NOT_FOUND(HttpStatus.NOT_FOUND, "알림을 찾을 수 없습니다.").

    6. NotificationServiceTest를 @SpringBootTest + @AutoConfigureMockMvc + @ActiveProfiles("local") + @Transactional로 작성. 시나리오: create 후 카운트 확인, markAsRead IDOR 방지, markAllAsRead 전체 읽음 처리.
  </action>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@21 ./mvnw test -pl backend -Dtest=NotificationServiceTest -f /Users/shinwon/confluence-clone-proj/pom.xml</automated>
  </verify>
  <acceptance_criteria>
    - Notification.java 파일이 존재하고 @Entity @Table(name="notifications") 어노테이션을 포함한다
    - NotificationRepository.java에 countByUserIdAndIsReadFalse 메서드가 존재한다
    - NotificationRepository.java에 markAllAsReadByUserId @Modifying @Query가 존재한다
    - NotificationService.java에 markAsRead( 메서드가 findByIdAndUserId를 사용하여 IDOR 방지 로직을 포함한다
    - NotificationServiceTest 테스트가 BUILD SUCCESS로 완료된다
  </acceptance_criteria>
  <done>Notification 도메인 전체(엔티티/리포지토리/DTO/서비스) 생성 완료, 통합 테스트 GREEN</done>
</task>

<task type="auto">
  <name>Task 2: CommentService에 NotificationService 통합</name>
  <files>
    backend/src/main/java/com/company/wiki/comment/service/CommentService.java
  </files>
  <read_first>
    - backend/src/main/java/com/company/wiki/comment/service/CommentService.java (createComment 전체 구현 확인 — content.getCreatedBy() 타입이 User 객체인지 Long인지 확인 필수)
    - backend/src/main/java/com/company/wiki/content/entity/Content.java (createdBy 필드 타입 확인)
    - backend/src/main/java/com/company/wiki/notification/service/NotificationService.java (Task 1에서 생성한 create() 시그니처 확인)
  </read_first>
  <action>
    1. CommentService에 NotificationService를 @RequiredArgsConstructor 주입한다.

    2. createComment() 메서드에서 comment = commentRepository.save(comment) 직후, return buildCommentNode() 직전에 다음 블록을 삽입한다:
       try {
         Long contentAuthorId = content.getCreatedBy().getId(); // Content.createdBy가 User 엔티티 참조인 경우
         // content.getCreatedBy()가 Long(userId)인 경우: Long contentAuthorId = content.getCreatedBy();
         if (!contentAuthorId.equals(userId)) { // 자기 글에 자기 댓글은 알림 제외
           notificationService.create(
             contentAuthorId,
             "COMMENT",
             "새 댓글이 달렸습니다",
             content.getTitle() + "에 댓글이 달렸습니다.",
             "/spaces/" + content.getSpaceId() + "/contents/" + contentId
           );
         }
       } catch (Exception e) {
         log.warn("알림 생성 실패 (비중단): {}", e.getMessage());
       }

    3. content.getCreatedBy()의 실제 타입(User 객체 vs Long)에 맞게 contentAuthorId 추출 방법을 조정한다. Content 엔티티를 반드시 읽은 후 정확한 필드 접근 방법을 사용한다.

    4. 이 Phase에서는 댓글 알림(콘텐츠 작성자 1명)만 구현한다. 멘션/구독자/메일수신 알림은 Phase 4 범위 외다 (CONTEXT.md 확정 결정).
  </action>
  <verify>
    <automated>JAVA_HOME=/opt/homebrew/opt/openjdk@21 ./mvnw compile -pl backend -f /Users/shinwon/confluence-clone-proj/pom.xml</automated>
  </verify>
  <acceptance_criteria>
    - CommentService.java에 notificationService.create( 호출이 존재한다
    - 해당 호출이 try-catch 블록으로 감싸져 있다
    - 자기 글에 자기 댓글인 경우 알림을 건너뛰는 조건 (!contentAuthorId.equals(userId)) 이 존재한다
    - mvnw compile이 BUILD SUCCESS로 완료된다
  </acceptance_criteria>
  <done>CommentService에 NotificationService 통합 완료, 댓글 생성 시 콘텐츠 작성자에게 알림 적재</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NotificationController (Wave 2) → NotificationService | 인증된 사용자 요청 — userId는 JWT에서 추출 |
| NotificationService.markAsRead() | IDOR 경계 — 다른 사용자의 알림을 읽음 처리하려는 시도 방지 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | NotificationService.markAsRead() | mitigate | findByIdAndUserId로 소유자 검증 — 불일치 시 NOTIFICATION_NOT_FOUND 예외 발생 |
| T-04-02-02 | Information Disclosure | GET /api/notifications | mitigate | NotificationController(Wave 2)에서 JWT 인증 userId만 조회 — 다른 사용자의 알림 목록 노출 불가 |
| T-04-02-03 | Denial of Service | NotificationService.create() 예외 | mitigate | CommentService에서 try-catch로 예외 삼킴 — 알림 실패가 댓글 저장을 롤백하지 않음 |
| T-04-02-SC | Tampering | npm/pip/cargo installs | accept | 이 플랜은 신규 패키지 설치 없음 |
</threat_model>

<verification>
1. NotificationServiceTest 실행 — BUILD SUCCESS, 테스트 모두 GREEN
2. CommentService 컴파일 성공 확인
3. (선택) 댓글 생성 후 `SELECT * FROM notifications WHERE type = 'COMMENT'`으로 레코드 확인
</verification>

<success_criteria>
- Notification 도메인(entity/repository/dto/service) 신규 생성 완료
- NotificationService.markAsRead()가 userId 소유자 검증 포함 (IDOR 방지)
- CommentService에서 NotificationService 직접 호출 통합 (try-catch 래핑)
- NotificationServiceTest 통합 테스트 GREEN
</success_criteria>

<output>
Create `.planning/phases/04-admin/04-02-SUMMARY.md` when done
</output>
